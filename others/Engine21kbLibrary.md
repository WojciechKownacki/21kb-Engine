# Engine21kbLibrary — plan biblioteki skryptowej runtime

## 1. Cel i granice

`Engine21kbLibrary` jest publiczną powierzchnią skryptowania gry dla trzech
front-endów: C++ (native behaviour), Lua oraz Visual Graph. Ma umożliwić
tworzenie pełnych gier, ale **nie jest drugim runtime'em, drugim ECS-em ani
kopią stanu sceny**.

Źródłem prawdy pozostają istniejące systemy silnika: `Scene`/ECS, runtime
renderera, fizyka, audio, input, asset registry i system skryptów.
`Engine21kbLibrary` ma być ich wersjonowanym, typowanym i sprawdzalnym
kontraktem. Każda operacja biblioteki albo wywołuje właściwy system, albo
odkłada jednoznaczną komendę do jego kolejki. Nie przechowuje ukrytego stanu,
który mógłby rozjechać się z runtime'em.

Aktualny silnik już posiada wartościowy fundament: `ScriptRuntimeHost`,
backendy Native/Lua/VisualGraph, `ScriptApiCatalog`, `ScriptApiNameRegistry`
oraz zdarzenia `Created`, `Activated`, `Ready`, `FixedTick`, `Tick`,
`LateTick`, `BeforeRender`, `AfterRender`, `Deactivated`, `Destroyed`.
Istnieją także pierwsze moduły `World`, `Transform`, `Time`, `Input`,
`Physics` i `Audio`. Ten plan rozwija je do produktu — nie zastępuje ich
równoległym mechanizmem.

### Niezmienialne zasady projektu

1. Jedna rejestracja funkcji opisuje nazwę, sygnaturę, uprawnienia, wątek,
   semantykę błędu oraz bindingi Lua i Visual Graph.
2. API operuje na stabilnych uchwytach (`EntityHandle`, `AssetRef`,
   `SceneRef`), nigdy na surowych wskaźnikach komponentów przechowywanych przez
   skrypt między klatkami.
3. Modyfikacje struktury ECS podczas iteracji są komendami zastosowanymi w
   zdefiniowanym punkcie synchronizacji; odczyty mają jasne zasady świeżości.
4. Kolejność skryptów, zdarzeń, timerów i zapytań jest deterministyczna w
   obrębie tej samej konfiguracji świata.
5. Funkcje niegotowe produkcyjnie nie są udawane. Są nieeksponowane albo
   zwracają jawny, diagnostyczny błąd kontraktu — nigdy cichy fallback.
6. Każda publiczna funkcja ma test native, test Lua, test bindingu Visual
   Graph oraz test błędu dla błędnych danych, jeśli dane wejściowe są możliwe.
7. Hot path nie wykonuje refleksji, alokacji ani wyszukiwania po stringu na
   każdą klatkę. Nazwy kompilują się/rejestrują do stabilnych identyfikatorów.

## 2. Docelowy model programowania

```text
Game code / Lua / Visual Graph
              │
              ▼
 Engine21kbLibrary (publiczne moduły, typy, kontrakty, uprawnienia)
              │  jednolity ScriptFunctionDesc / binding catalog
              ▼
 ScriptRuntimeHost + kolejki komend + scheduler lifecycle
              │
              ▼
 Scene/ECS | Input | Physics | Renderer | Audio | Assets | Network | UI
```

Przykładowy kod gry ma mieć tę samą intencję w każdym frontendzie:

```cpp
void PlayerController::FixedTick(Engine21kbLibrary::FixedContext& ctx) {
    const auto move = ctx.input.Action2D("Move");
    ctx.physics.MoveKinematic(ctx.self, move * speed * ctx.time.fixedDelta);
    if (ctx.input.Pressed("Fire")) {
        ctx.world.Spawn(prefabs.projectile, ctx.transform.WorldPose(ctx.self));
    }
}
```

Wizualny węzeł `Input.Action2D → Physics.MoveKinematic` i Lua
`Input.action2d("Move")` muszą wykonywać tę samą operację backendową, a nie
trzy niezależne implementacje.

## 3. Lifecycle i punkty synchronizacji

### Kontrakt świata

| Faza | Kiedy | Dozwolone działanie |
|---|---|---|
| `Created` | po utworzeniu instancji behaviour | inicjalizacja danych lokalnych; brak założenia aktywności |
| `Activated` | entity i komponent aktywne w świecie | subskrypcje, rejestracja timerów |
| `Ready` | po aktywacji wszystkich zależności sceny | odczyt innych obiektów, rozpoczęcie gry |
| `FixedTick` | 0..N razy przed symulacją renderowaną | deterministyczna logika/fizyka; stałe `fixedDelta` |
| `Tick` | raz na klatkę po kroku stałym | input, gameplay zależny od frame delta |
| `LateTick` | po `Tick` wszystkich behaviours | kamera, śledzenie, uporządkowane zależności |
| `BeforeRender` | przed zgłoszeniem renderowania | przygotowanie prezentacji, bez blokowania GPU |
| `AfterRender` | po zgłoszeniu renderowania | telemetry/debug, bez mutacji render-listy bieżącej klatki |
| `Deactivated` | przed utratą aktywności | anulowanie subskrypcji/timerów |
| `Destroyed` | jednoznacznie przed zwolnieniem | zwolnienie zasobów script-local; idempotentne cleanup |

Kolejność jest: `Created → Activated → Ready → (FixedTick*) → Tick →
LateTick → BeforeRender → AfterRender → Deactivated → Destroyed`. `Ready` nie
może wykonać się dwa razy dla jednej aktywacji bez jawnej semantyki restartu.

### Pionowy wycinek, który kończy lukę „edytor bez gry”

- [x] LIB-001 Opisać publiczny namespace, eksporty i politykę wersjonowania `Engine21kbLibrary`. Dowód: `sources/engine/include/engine/library/EngineLibrary.hpp` (namespace `kb::library`, doc kontraktu eksportów, `LibraryApiVersion{major,minor,patch}` + `kEngineLibraryApiVersion{0,1,0}` + `CanRun()`), `sources/engine/src/library/EngineLibrary.cpp` (`ToString`), test `sources/engine/tests/EngineLibraryTests.cpp` (suite `engine-library`, zarejestrowana w `TestSuites.hpp`/`SceneRuntimeTests.cpp`/CMakeLists). `cmake --build build --target kb_engine_tests --config Debug` — build bez warningów; `kb_engine_tests.exe engine-library` i pełny `kb_engine_tests.exe` (wszystkie suity) — exit 0.
- [x] LIB-002 Utworzyć pojedynczy moduł rejestracji biblioteki wywoływany przez `ScriptRuntimeHost` przy tworzeniu świata. Dowód: `sources/engine/include/engine/library/EngineLibraryModule.hpp` + `sources/engine/src/library/EngineLibraryModule.cpp` (`kb::library::EngineLibraryModule::Install(host)` zastąpiło 6 rozproszonych wywołań `Script*Api::Register` w `ScriptRuntimeHost::RegisterDefaultBackends()`, patrz `sources/engine/src/script/ScriptRuntimeHost.cpp`). Testy: `sources/engine/tests/EngineLibraryTests.cpp::RunModuleInstallCoversAllDomainsTest` (Input/Audio/World/Time/Physics/Transform osiągalne przez jeden entry point) i `RunModuleInstallReportsDuplicateDiagnosticsTest` (błąd = 6 diagnostyk, nie cichy fallback). `cmake --build build --target kb_engine_tests --config Debug` — bez warningów; `kb_engine_tests.exe engine-library` oraz pełny `kb_engine_tests.exe` (wszystkie suity) — exit 0.
- [x] LIB-003 Zastąpić ręczne, rozproszone rejestracje modułów jednym katalogiem z właścicielem, sygnaturą i capability. Dowód: po domknięciu LIB-016/017, `EngineLibraryModule::Install()` (`sources/engine/src/library/EngineLibraryModule.cpp`) nie ma już hardkodowanej sekwencji 6 wywołań — iteruje `Catalog()` (`std::vector<LibraryModuleDesc>`), pomijając moduły z `capability=false` zamiast je fałszywie rejestrować. `InstallModules(host, modules)` wystawione publicznie do wstrzykiwania własnego zestawu (testy, przyszłe LIB-020). Testy: `RunModuleCatalogTest` (kształt katalogu), `RunModuleInstallSkipsUnavailableCapabilityTest` (capability=false nigdy nie woła Register). Build+testy zielone.
- [x] LIB-004 Zmapować obecne `ScriptLifecycleEvent` na publiczne konteksty lifecycle bez duplikowania scheduler'a. Dowód: `sources/engine/include/engine/library/EngineLibraryLifecycle.hpp` (`kb::library::LifecycleEvent` = alias `kb::script::ScriptLifecycleEvent`, `LibraryLifecycleContextKind{Behaviour,Fixed,Frame,Render}`, `ClassifyLifecycleContext()`), `sources/engine/src/library/EngineLibraryLifecycle.cpp` (`ToString`). Test: `EngineLibraryTests.cpp::RunLifecycleContextClassificationTest` (10/10 zdarzeń zgodnych z tabelą z sekcji 3 planu + `static_assert` braku duplikacji enuma). Build+testy zielone (jak wyżej), exit 0.
- [x] LIB-005 Zdefiniować gwarantowaną kolejność `executionOrder`, następnie stabilny identyfikator entity i komponentu. Dowód: kontrakt sortowania (`BehaviourTickGroup` asc → `BehaviourComponent::executionOrder` asc → entity id asc) był zduplikowany w `ScriptRuntime.cpp::ComesBefore` i `ScriptRuntimeSceneSystem.cpp::CollectBehaviourRecords` — wydzielony do jednej `inline` funkcji `kb::scene::BehaviourExecutionOrderLess` (`sources/engine/include/engine/scene/BehaviourExecutionOrder.hpp`, header-only — hot path, sortowany co klatkę, brak LTO w buildzie), oba miejsca ją teraz wołają. Naprawiona realna luka determinizmu: `SyncBehaviourLifecycles`/`ShutdownTrackedBehaviours` w `ScriptRuntimeSceneSystem.cpp` iterowały po `unordered_map` przy wysyłce `Deactivated`/`Destroyed` dla usuwanych behaviours — teraz sortowane tym samym komparatorem przed dispatchem. `kb::library` wystawia kontrakt w `sources/engine/include/engine/library/EngineLibraryExecutionOrder.hpp` (`TickGroup`, `BehaviourExecutionOrderLess`, `EntityId` = `kb::ecs::Entity::IdType` stabilne w ramach sesji z generation-counter przez flecs, `ComponentId` = `kb::ecs::ComponentId` udokumentowane jako stabilne TYLKO w ramach procesu — luka do zamknięcia w LIB-009). Test: `EngineLibraryTests.cpp::RunExecutionOrderContractTest`. `cmake --build build --target kb_engine_tests --config Debug` — bez warningów; `kb_engine_tests.exe engine-library`, `kb_engine_tests.exe script` (pełna suita ScriptRuntimeTests po refaktorze) i pełny `kb_engine_tests.exe` — exit 0.
- [x] LIB-006 Ustalić punkt zastosowania komend ECS dla każdej fazy i przetestować go. Dowód: research wykazał, że dziś istnieją dwa niepołączone mechanizmy — `kb::ecs::World` (natychmiastowe mutacje, chronione tylko przez `StructuralChangeValidator` rzucający przy strukturalnej zmianie w trakcie otwartej iteracji Query) i `kb::ecs::CommandBuffer` (w pełni odłożony, jawny `Playback()`, ale niepodłączony do żadnej fazy lifecycle). Skrypty (`World.Spawn`/`World.Destroy` w `ScriptWorldApi.cpp`) używają ścieżki natychmiastowej i są bezpieczne wyłącznie dzięki wzorcowi "collect-then-dispatch" w `ScriptRuntime::DispatchSceneBehaviours`/`ScriptRuntimeSceneSystem::SyncBehaviourLifecycles` (zbiórka przez `ForEach` kończy się PRZED wywołaniem jakiegokolwiek skryptu). Kontrakt udokumentowany i wystawiony w `sources/engine/include/engine/library/EngineLibraryCommandApplication.hpp` (`CommandApplicationPoint{Immediate,DeferredUntilPlayback}`, `CommandApplicationPointFor(phase)` — dziś `Immediate` dla każdej fazy). Test: `EngineLibraryTests.cpp::RunCommandApplicationContractTest` — realny native behaviour spawnujący entity w Tick, dowodzi że (1) nowe entity jest natychmiast żywe/czytelne w tej samej fazie, (2) NIE dostaje własnych zdarzeń lifecycle w tej samej klatce (bo snapshot dispatchu tej fazy był już zebrany), (3) dostaje `Created`+`Tick` dopiero od następnej klatki. Build+testy zielone (jak wyżej), exit 0. Luka do przyszłych zadań: brak testu wzorca "CommandBuffer nagrywany w trakcie iteracji Query, playback po pętli" — to jawny zakres LIB-083, nadal nieukończone.
- [x] LIB-007 Udostępnić `BehaviourContext`, `FixedContext`, `FrameContext` i `RenderContext` bez referencji o niepewnym życiu. Dowód: `sources/engine/include/engine/library/EngineLibraryContext.hpp` — cztery typy finalne dziedziczące z `LibraryContextBase` (non-owning wskaźnik na call-scoped `ScriptExecutionContext`, żyjący wyłącznie na czas jednego callbacku; jedyna wartość bezpieczna do przechowania to `Self()` zwracające `EntityId` — zwykłą 64-bit wartość z LIB-005, nie referencję). `BehaviourContext` (Created/Activated/Ready/Deactivated/Destroyed) bez danych czasowych; `FixedContext.FixedDeltaSeconds()`/`FrameContext.DeltaSeconds()` dla FixedTick/Tick+LateTick; `RenderContext` bez mutatora render-listy (zgodnie z zakazem z sekcji 3 planu). Test: `EngineLibraryTests.cpp::RunLibraryContextTest` — realny native behaviour dispatchowany przez `ScriptRuntimeSceneSystem::ExecuteFrame`, weryfikuje poprawny typ kontekstu i wartości dla Created/FixedTick/Tick/BeforeRender. Build+testy zielone, exit 0.
- [x] LIB-008 Wprowadzić `EntityHandle` z generacją, kontrolą świata i diagnostyką nieaktualnego uchwytu. Dowód: `sources/engine/include/engine/library/EngineLibraryEntityHandle.hpp` + `sources/engine/src/library/EngineLibraryEntityHandle.cpp` — `EntityHandle{entity, sceneId}` (wartościowy, bez wskaźnika do Scene/World, wzorem `ScenePrefabHandle`), generacja opakowana przez istniejący `kb::ecs::Entity`/flecs (LIB-005, nie zduplikowana), kontrola świata przez porównanie `Scene::Id()`, diagnostyka `Validate()` rzucająca `std::invalid_argument` (nieprawidłowy uchwyt) / `std::out_of_range` (inna scena / nieaktualny uchwyt) wzorem istniejącego `World::ValidateEntityHandle`. `LibraryContextBase::SelfHandle()` (`EngineLibraryContext.hpp`) zwraca pełny handle, `Self()` (LIB-007) pozostał bez zmian. Test: `EngineLibraryTests.cpp::RunEntityHandleTest` (żywy/martwy/invalid/inna scena/po recyklingu indeksu) + rozszerzenie `RunLibraryContextTest` o `SelfHandle()`. Build+testy zielone, exit 0.
- [x] LIB-009 Wprowadzić `AssetRef<T>` oraz `SceneRef` z identyfikatorem runtime, a nie ścieżką systemową. Dowód: `sources/engine/include/engine/library/EngineLibraryAssetRef.hpp` — `AssetRef<T>` to alias `kb::assets::AssetHandle<T>` (nie nowa warstwa: istniejący `AssetId` to już deterministyczny hash logicznej ścieżki+typu, `AssetManager` już ma cache/refcounting), `SceneRef = AssetRef<kb::scene::SceneDocument>` korzystający z istniejącego `SceneAssetLoader` — jawnie odróżniony od `kb::scene::Scene::Id()` (runtime instance z LIB-008). Test: `EngineLibraryTests.cpp::RunAssetRefTest` — zapisuje realny plik `.21kbscene`, mountuje projekt, discover, ładuje przez `AssetManager::Load<SceneDocument>`, weryfikuje payload i że `AssetId` jest odtwarzalny jako `MakeAssetId(NormalizeAssetPath(virtualPath)+":"+type)` — niezależnie od physicalPath. Build+testy zielone, exit 0.
- [x] LIB-010 Ujednolicić wyniki błędów: `Result<T, ScriptError>` dla operacji zawodnych oraz wyjątek/diagnostykę adaptera dla Lua. Dowód: `kb::library::Result<T>` + `ScriptError` (`EngineLibraryResult.hpp`, `EngineLibraryError.hpp`/`.cpp`) — jedna, spójna forma wyniku zawodnej operacji `kb::library` (Ok/Fail, nie duplikuje osobnych struct-ów `ScriptFunctionCallResult`/`EngineLibraryModuleResult`, tylko nazywa wzorzec). Realna, krytyczna naprawa bezpieczeństwa: research wykazał, że wyjątek C++ rzucony przez zarejestrowany callback (np. przyszłe `EntityHandle::Validate()`) przechodzący przez `lua_pcall` (PUC-Lua skompilowane jako C, longjmp-based) to dziś UB/crash procesu, nie diagnostyka. Naprawione w DWÓCH miejscach: `ScriptFunctionRegistry::Call` (`sources/engine/src/script/ScriptFunctionRegistry.cpp`, jedyny punkt przez który przechodzą Native, Lua `CallFunction` i przyszły Visual Graph) oraz `NativeScriptBackend::ExecuteLifecycle`/`ExecuteEvent` (`sources/engine/src/script/NativeScriptBackend.cpp`, bezpośredni dispatch behaviourów) — wyjątek konwertowany na `ScriptFunctionCallResult.errors`/`ScriptDiagnostic`, pętla dispatchu pozostałych behaviourów w tej samej fazie nie jest przerywana. Testy: `ScriptRuntimeTests.cpp::RunScriptFunctionRegistryExceptionSafetyTest` (rejestr zostaje używalny po wyjątku), `RunNativeScriptBackendExceptionSafetyTest` (behaviour po throwing-behaviourze w tej samej fazie nadal dostaje Tick — dowód że pętla przeżywa), `EngineLibraryTests.cpp::RunResultTest`. `cmake --build build --target kb_engine_tests --config Debug` — bez warningów; `kb_engine_tests.exe` (`engine-library`, `script`, pełny run) — exit 0 (co samo w sobie dowodzi, że proces NIE ulega crashowi/terminate przy testowanym wyjątku — kluczowy dowód naprawy).
- [x] LIB-011 Dodać test kolejności lifecycle dla Native, Lua i Visual Graph. Dowód: `sources/engine/tests/ScriptRuntimeTests.cpp` — `RunNativeFullLifecycleOrderTest`, `RunPucLuaFullLifecycleOrderTest`, `RunVisualGraphFullLifecycleOrderTest` (każdy weryfikuje pełną sekwencję 10 zdarzeń Created→Destroyed w jednym ciągłym porównaniu, przez dwa kolejne `ExecuteFrame` — poprzednie testy pokrywały tylko fragmenty) oraz `RunCrossBackendLifecycleOrderParityTest` (Native+Lua+VisualGraph w jednej scenie, utworzone w kolejności odwrotnej do oczekiwanej, dowodzi że `BehaviourExecutionOrderLess` obowiązuje niezależnie od backendu). Build+testy zielone (`kb_engine_tests.exe`, suite `script`, pełny run) — exit 0.
- [x] LIB-012 Dodać test powtórnej aktywacji, niszczenia w tej samej klatce i anulowania pending commands. Dowód: (1) powtórna aktywacja bez rekreacji już pokryta przez istniejący `RunScriptRuntimeSceneSystemDynamicLifecycleTest`; (2) niszczenie wielu behaviourów w tej samej klatce pokryte przez `RunMultipleBehavioursRemovedSameFrameOrderTest` (dodany podczas audytu LIB-005); (3) anulowanie pending commands — NOWY test `sources/engine/tests/ScriptRuntimeTests.cpp::RunPendingCommandCancelledByDestroyTest`: entity wysyła targetowany event do innej entity i natychmiast ją niszczy w tej samej klatce — dowodzi, że `ScriptRuntime::DrainEvents`' `pending` queue bezpiecznie porzuca dostawę do zniszczonego celu (świeża kolekcja behaviourów przy każdym dispatchu), zamiast dostarczać do nieaktualnej/wiszącej encji lub crashować. Build+testy zielone (`kb_engine_tests.exe`, suite `script`, pełny run) — exit 0.
- [x] LIB-013 Dodać minimalny template `PlayerController` działający w Play Mode bez zależności edytora. Dowód: research potwierdził, że jedyną istniejącą, faktycznie edytoro-niezależną powierzchnią "Play Mode" jest `kb-cli run` (headless, zero bgfx/okna/edytora, prawdziwa pętla klatek przez `ScriptRuntimeHost`/`ScriptRuntimeSceneSystem`) — `EditorPlayModeSceneSession`/`EditorPlayModeState` żyją w `sources/editor` i są jawnie tym, czego zadanie zabrania. `sources/tools/tests/KbCliTests.cpp::RunPlayerControllerTemplateTests` — realny `PlayerController.lua` (Input.Vector2 → Transform.Translate, wzorzec z `ScriptAgentProjectFiles`' AGENTS.md), zapisana scena z node'em `Player`, uruchomione w procesie przez prawdziwe komendy CLI: `validate` → `scene-attach` → `run --frames 5`. Test weryfikuje: exit 0, `Ready` odpalone raz, `Tick` odpalone dokładnie 5× (raz na klatkę), event `PlayerMoved` wyemitowany, `0 diagnostics`. `Input.Vector2` bezpiecznie zwraca zera bez skonfigurowanego mapowania (potwierdzone w `InputSubsystem::GetActionValue`), więc headless run nie crashuje mimo braku urządzenia wejścia. Build+test zielone (`kb_cli_tests.exe`) — exit 0. Luka odnotowana (nie blokuje LIB-013): `Physics.*` ma dziś tylko `Raycast`, brak `MoveKinematic`/velocity — ruch idzie przez `Transform.Translate`, zgodnie z aktualnym stanem API; brak windowed/renderer standalone playera (poza zakresem "bez edytora, headless").
- [ ] LIB-014 Dodać minimalny template `Projectile` z prefabem, ruchem, kolizją i zniszczeniem. ZABLOKOWANE: "kolizja" wymaga script-facing collision event API (`OnCollisionEnter/Stay/Exit`), które nie istnieje przed LIB-127 (sekcja 10, dziś `ScriptPhysicsApi` ma tylko `Physics.Raycast`, potwierdzone grepem — zero `OnCollision*`/`OnTrigger*` w `sources/engine/src/script`). Patrz `others/_temp.md` POWRÓT.
- [ ] LIB-015 Dodać sample scene i test end-to-end: input → ruch → spawn → kolizja → audio/log. ZABLOKOWANE: zależy od LIB-014 (kolizja), ten sam brakujący prerequisite.

## 4. Architektura i kontrakty bazowe

### 4.1 Katalog bindingów i metadata

- [x] LIB-016 Zdefiniować `LibraryModuleDesc`: moduł, wersja, owner runtime, capability i zależności. Dowód: `sources/engine/include/engine/library/EngineLibraryModuleDesc.hpp` — `name`, `LibraryModuleVersion`, `ownerRuntime` (realna etykieta zweryfikowanego właściciela stanu, np. `kb::scene::SceneEntities` dla World — sprawdzone w kodzie, nie zgadywane), `capability`, `dependencies`. Realnie skonsumowane przez `EngineLibraryModule::Catalog()`/`Install()` (LIB-003). Test: `RunModuleCatalogTest`.
- [x] LIB-017 Zdefiniować `LibraryFunctionDesc`: kanoniczna nazwa, wejścia, wyjścia, determinism, thread affinity i błąd. Dowód: `sources/engine/include/engine/library/EngineLibraryFunctionDesc.hpp` — `canonicalName` (odnosi się do `ScriptFunctionSignature::name`, nie duplikuje wejść/wyjść, które już tam żyją), `LibraryThreadAffinity`, `LibraryDeterminism`, `canFail`. Pole `LibraryModuleDesc::functions` (przyrostowo audytowane — pusta lista = "jeszcze nie zaudytowane", nie "nie istnieje"; pełny audyt wszystkich funkcji to LIB-231/LIB-237, nie to zadanie). Pilotażowo wypełnione i realnie zweryfikowane dla `World.Exists` (czysta funkcja nad `SceneEntities::IsAlive`, deterministyczna, main-thread, nie generuje błędu). Test anti-drift: `RunFunctionDescCatalogResolvesTest` — każdy audytowany `canonicalName` musi rozwiązać się w rzeczywiście zarejestrowaną funkcję przez `ScriptApiCatalog`.
- [x] LIB-018 Zdefiniować `LibraryTypeDesc`: serializacja, porównanie, domyślna wartość, Visual Graph pin i Lua marshalling. Dowód: `sources/engine/include/engine/library/EngineLibraryTypeDesc.hpp` — cienki agregator nad JUŻ istniejącymi, realnymi implementacjami (`kb::script::ToString` = serializacja, `kb::script::ToVisualGraphValueType` = Visual Graph pin, `luaTypeName` = dokumentacja marshallingu z `PucLuaValueBridge` — prywatny nagłówek, więc nazwa jest deklaratywna, nie wywołuje kodu, zgodnie z granicą public/private), `defaultValue`. Realna naprawa braku: `kb::script::ScriptValue` NIE MIAŁO `operator==` — dodane (`ScriptValue.hpp`), bo "porównanie" bez działającej implementacji byłoby pustą deklaracją. `DescribeType()` zwraca deskryptor dla wszystkich 7 `ScriptValueType`. Test: `EngineLibraryTests.cpp::RunTypeDescTest` — krzyżowa weryfikacja z `ToString`/`ToVisualGraphValueType`, oraz `ScriptValue::operator==` (w tym rozróżnienie Entity vs Component mimo tego samego surowego id). Build+testy zielone (`kb_engine_tests.exe` + `kb_cli_tests.exe`, oba dotknięte zmianą w `ScriptValue.hpp`) — exit 0.
- [x] LIB-019 Zdefiniować `LibraryPropertyDesc` dla bezpiecznych pól komponentów i assetów. Dowód: `sources/engine/include/engine/library/EngineLibraryPropertyDesc.hpp` — `LibraryPropertyDesc = kb::script::ScriptApiCatalogProperty` (alias, nie duplikat — ten typ już opisuje name/type/writable dla właściwości komponentów rejestrowanych w `ScriptSceneComponentApi.cpp`). Właściwości assetów nie mają dziś żadnego script-facing akcesora (LIB-155+ Assets), więc alias czeka gotowy na ich przyszłe użycie zamiast wprowadzać drugi kształt. Test: `EngineLibraryTests.cpp::RunPropertyDescTest` — `static_assert` tożsamości typu + realne właściwości Transform z żywego `ScriptApiCatalog` używane wprost jako `LibraryPropertyDesc`. Build+testy zielone.
- [x] LIB-020 Dodać walidację kolizji nazw, zmian sygnatur i cykli zależności modułów podczas startu. Dowód: `sources/engine/include/engine/library/EngineLibraryModuleValidation.hpp` + `.cpp` — `ValidateModuleCatalog()` sprawdza: puste/zduplikowane nazwy modułów, zależności wskazujące na nieistniejący moduł (literówki), cykle zależności (DFS 3-kolorowy), oraz funkcję (`LibraryFunctionDesc::canonicalName`) zaudytowaną jednocześnie przez dwa różne moduły ("zmiana sygnatury"/kolizja własności). Wpięte w `EngineLibraryModule::InstallModules()` PRZED jakąkolwiek rejestracją — nieprawidłowy katalog nie rejestruje NIC (fail-fast), zamiast częściowej rejestracji niespójnego zestawu. Testy: `RunModuleCatalogValidatesTest` (prawdziwy katalog produkcyjny przechodzi), `RunModuleValidationDuplicateNameTest`, `RunModuleValidationUnknownDependencyTest`, `RunModuleValidationCycleTest` (w tym pozytywny przypadek acyklicznego łańcucha), `RunModuleValidationDuplicateFunctionTest`, `RunModuleInstallFailsFastOnInvalidCatalogTest` (dowodzi że `Register()` NIGDY nie jest wołane dla złego katalogu). Build+testy zielone.
- [x] LIB-021 Zablokować rejestrację po starcie świata lub zdefiniować bezpieczną transakcję hot-reload. Decyzja: prostszy, bezpieczniejszy wariant "zablokować" — pełna transakcja hot-reload to osobne, dedykowane zadanie (LIB-228, M2). Dowód: `kb::script::ScriptFunctionRegistry` (`ScriptFunctionRegistry.hpp`/`.cpp`) — `Lock()`/`IsLocked()`, `Register()` odrzuca (zwraca `false`) po zablokowaniu. `ScriptRuntime::ExecuteLifecycle`/`ExecuteLifecycleForBehaviour`/`DispatchEvent` (jedyne 3 fundamentalne punkty dispatchu, przez które przechodzą wszystkie warianty `...AndDispatchEvents`) wołają `functions_.Lock()` jako pierwszą instrukcję — rejestracja jest legalna podczas setupu (konstrukcja `ScriptRuntimeHost`, `configureHost`), zablokowana od pierwszego realnego dispatchu fazy/zdarzenia świata. Test: `ScriptRuntimeTests.cpp::RunScriptFunctionRegistryLocksAfterFirstDispatchTest` — rejestracja przed dispatchem działa, po pierwszym dispatchu jest odrzucana (i nie dodaje funkcji), funkcje zarejestrowane wcześniej pozostają w pełni wywoływalne. Cała suita testów (obejmująca dziesiątki testów wywołujących dispatch) przeszła bez zmian — potwierdza, że istniejący kod już naturalnie rejestruje przed dispatchem. Build+testy zielone (`kb_engine_tests.exe` + `kb_cli_tests.exe`).
- [x] LIB-022 Generować manifest API dla edytora, dokumentacji, autocompletion i testów kompatybilności. Dowód (infrastruktura już istniała z wcześniejszej sesji — zweryfikowana w tej turze jako realnie spełniająca kryterium): `kb::script::ScriptApiExport::ToMarkdown/ToJson/ToLuaStubs` (`sources/engine/src/script/ScriptApiExport.cpp`) konsumuje `ScriptApiCatalog::Build(host)` i generuje: Markdown (dokumentacja), JSON (dane strukturalne dla edytora/przyszłych testów kompatybilności LIB-024), Lua stuby `---@meta` (autocompletion, zgodne z Lua Language Server przez `.luarc.json`). Dostępne przez `kb-cli api --print markdown|json|lua` i `kb-cli init-agent` (zapisuje `.kb/api/{kb.lua,script_api.md,script_api.json}` + `AGENTS.md`). Testy: `ScriptApiCatalogTests.cpp::RunCatalogExportTest` (treść wszystkich 3 formatów, w tym walidacja że Lua stuby faktycznie parsują się jako poprawny Lua), `KbCliTests.cpp::RunApiCommandTests`. Nie wymagało nowego kodu w tej turze — pozostałe braki (hash ABI/API do artefaktu builda, porównanie w CI) to jawnie osobne LIB-023/LIB-024.
- [x] LIB-023 Zapisać manifest API do artefaktu builda z hash'em ABI/API. Dowód: `sources/engine/include/engine/library/EngineLibraryManifest.hpp`/`.cpp` — `ComputeApiManifestHash()` (deterministyczny FNV-1a 64-bit, 16 cyfr hex), `BuildApiManifest(catalog)` (paruje `kEngineLibraryApiVersion` z hashem kanonicznego JSON z `ScriptApiExport::ToJson`), `ToJson(manifest)`. Wpięte w `kb-cli api`/`kb-cli init-agent` (`sources/tools/src/CliApiCommands.cpp`) — zapisują `.kb/api/manifest.json` jako część artefaktu builda obok istniejących `kb.lua`/`script_api.md`/`script_api.json`. Architektura: `kb::script` (ScriptAgentProjectFiles) NIE zależy od `kb::library` (zachowana granica wrap-only) — hash dopisuje narzędzie CLI, jedyna warstwa uprawniona łączyć obie strony. Testy: `EngineLibraryTests.cpp::RunApiManifestTest` (determinizm, wrażliwość na zmianę treści, spójność z `ToString(version)`), `KbCliTests.cpp::RunApiCommandTests` (manifest.json zapisany, identyczny hash przy dwóch kolejnych `init-agent` na niezmienionym projekcie). Build+testy zielone (`kb_engine_tests.exe` + `kb_cli_tests.exe`).
- [ ] LIB-024 Porównywać manifest poprzedniej wersji w CI i wykrywać breaking changes. CZĘŚCIOWO: silnik porównania gotowy i przetestowany — `sources/engine/include/engine/library/EngineLibraryManifestComparison.hpp`/`.cpp`: `CompareApiCatalogs(baseline, current)` klasyfikuje każdą różnicę (usunięta/zmieniona funkcja lub property = Breaking, tylko dodana funkcja/component/property = Additive), `ApiCompatibilityReport::HasBreakingChanges()`. Testy: `EngineLibraryTests.cpp::RunApiCompatibilityComparisonTest` (identyczny katalog = brak zmian; usunięta funkcja/zmieniony typ pinu/property stające się read-only = Breaking; nowa funkcja = Additive, nie Breaking). ZABLOKOWANE dla realnego wpięcia "w CI" (`.github/workflows/ci.yml`): wymaga decyzji o strategii przechowywania baseline'u między uruchomieniami CI (np. commitowany plik referencyjny w repo + osobny workflow do jego aktualizacji, czy pobieranie artefaktu z poprzedniego builda brancha bazowego) — to decyzja dot. współdzielonej infrastruktury, nie kodu; nie modyfikowałem `.github/workflows/ci.yml` bez tej decyzji. Patrz `others/_temp.md` POWRÓT.
- [x] LIB-025 Dodać atrybut `Deprecated` z migratorem Visual Graph oraz komunikatem dla Lua/C++. Dowód: `sources/engine/include/engine/library/EngineLibraryDeprecation.hpp`/`.cpp` — `LibraryDeprecation{message, replacementCanonicalName, sinceVersion}`, `FormatDeprecationWarning()` (jeden wspólny komunikat, więc Lua i C++ widzą identyczny tekst — nie ma osobnych ścieżek do rozjazdu), `MigrateVisualGraphCallNativeNodes()` — mechaniczne przepisanie klucza bindingu `"Function."+stara` → `"Function."+nowa` dla węzłów `CallNative` w grafie (klucz zgodny z auto-generowanym bindingiem z `ScriptFunctionVisualGraphBindings`), świadomie NIE migruje niczego, gdy deprecation nie deklaruje zamiennika (usunięta funkcjonalność ma zostać zgłoszona człowiekowi, nie po cichu przepisana na coś innego). Test: `EngineLibraryTests.cpp::RunDeprecationTest`. Build+testy zielone.
- [x] LIB-026 Zdefiniować stabilne identyfikatory funkcji niezależne od kolejności rejestracji. Dowód: `sources/engine/include/engine/library/EngineLibraryFunctionId.hpp`/`.cpp` — `ComputeLibraryFunctionId(canonicalName)` (FNV-1a 64-bit hash nazwy, ten sam wzorzec co `ComputeApiManifestHash`/`kb::assets::MakeAssetId`), deterministyczny i niezależny od pozycji w `ScriptFunctionRegistry::Functions()` czy kolejności rejestracji modułów — kontrast z `kb::ecs::ComponentId` (LIB-005), które JEST zależne od kolejności rejestracji w danym procesie. Nie zastępuje wyszukiwania po nazwie (`ScriptFunctionRegistry::Call` nadal rozwiązuje po stringu) — to rezerwa pod przyszłe zwarte klucze (Visual Graph binary IR, LIB-235). Test: `EngineLibraryTests.cpp::RunFunctionIdTest` (determinizm, różne nazwy → różne id, identyczne id z dwóch niezależnie skonstruowanych hostów). Build+testy zielone.
- [x] LIB-027 Zdefiniować politykę feature flags: capability ujawniona tylko, gdy backend istnieje. Dowód: zaimplementowane jako część LIB-016/LIB-003 — `LibraryModuleDesc::capability` (`EngineLibraryModuleDesc.hpp`) + `EngineLibraryModule::InstallModules()` pomija moduł z `capability=false` (nigdy nie woła `Register()`, nie zgłasza diagnostyki — to nie błąd, to świadomie wyłączona funkcja). Test: `EngineLibraryTests.cpp::RunModuleInstallSkipsUnavailableCapabilityTest` (dowodzi, że `Register()` faktycznie nigdy nie jest wołane dla capability=false) oraz `RunModuleCatalogTest` (wszystkie 6 modułów produkcyjnych mają `capability=true`, bo ich backend jest realnie skompilowany w tym buildzie).
- [x] LIB-028 Dodać report startowy: dostępne moduły, wyłączone capability, wersje i przyczyna. ZNALEZIONE PODCZAS AUDYTU 2026-07-13: zadanie było nieoznaczone i BEZ adnotacji "ZABLOKOWANE"/"CZĘŚCIOWE" (w przeciwieństwie do LIB-024/029/039/040) — realna luka, nie świadome odłożenie; naprawione natychmiast. Dowód: `EngineLibraryModuleReportEntry{name, version, ownerRuntime, capability, installed, reason}` (`EngineLibraryModule.hpp`) — `EngineLibraryModuleResult::report`, JEDEN wpis na moduł katalogu, W KOLEJNOŚCI katalogu, niezależnie od wyniku (nie tylko lista błędów jak stare `diagnostics`). `InstallModules()` wypełnia `report` realnie: `installed=true` tylko gdy `capability=true` I `Register()` faktycznie się powiódł; dla `capability=false` — `reason` = nowe pole `LibraryModuleDesc::disabledReason` (jeśli podane) albo generyczny fallback; dla nieudanej walidacji katalogu (LIB-020) — KAŻDY moduł oznaczony jako niezainstalowany ze wspólną przyczyną (report nie zostaje pusty tylko dlatego że nic pojedynczo nie zawiodło). `FormatStartupReport(report)` — prawdziwy renderer do czytelnego tekstu (nazwa, wersja x.y.z, [installed]/[disabled]+przyczyna), nie tylko surowa struktura której nikt nigdy nie drukuje. Wpięcie w PRAWDZIWY runtime: `ScriptRuntimeHost::LibraryStartupReport()` — nowy publiczny akcesor zwracający raport z faktycznego wywołania `EngineLibraryModule::Install()` które ten host wykonał przy konstrukcji (nie ponownie przeliczony) — każdy `Scene`+`ScriptRuntimeHost` w silniku (edytor, standalone player, kb_cli, testy) ma teraz ten raport realnie dostępny. Test: `EngineLibraryTests.cpp::RunModuleInstallStartupReportTest` — własny mini-katalog (1 moduł capability=true+Register sukces, 1 moduł capability=false+jawny `disabledReason`) dowodzący dokładnej zawartości wpisów (nazwa/wersja/reason), `FormatStartupReport` dowiedzione na zawieranie realnych nazw/wersji/przyczyn w wyjściowym tekście, oraz PRAWDZIWY `ScriptRuntimeHost::LibraryStartupReport()` na zwykłym hoście pokazujący wszystkie 7 modułów produkcyjnego katalogu jako `installed=true` (bo wszystkie mają `capability=true` w tym buildzie). Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wtyczki, zero błędów) oraz WSZYSTKIE 10 binarek testowych (`kb_engine_tests`, `kb_ecs_api_tests`, `kb_ecs_deterministic_replay_tests`, `kb_ecs_scheduler_correctness_tests`, `kb_ecs_stress_tests`, `kb_ecs_snapshot_scale_tests`, `kb_renderer_tests`, `kb_editor_tests`, `kb_editor_material_graph_canvas_tests`, `kb_cli_tests`) exit 0 + `kb_render_smoke.exe` (prawdziwy runtime, 600 wyrenderowanych klatek) exit 0.
- [ ] LIB-029 Dodać test zgodności katalogu: każda funkcja ma opis i binding we wszystkich wspieranych frontendach. CZĘŚCIOWO: część "binding we wszystkich frontendach" zrobiona i przetestowana — `EngineLibraryTests.cpp::RunCatalogFunctionsHaveVisualGraphBindingsTest` iteruje WSZYSTKIE funkcje z żywego `ScriptApiCatalog` i weryfikuje realny binding Visual Graph (native+runtime) oraz wpis w rejestrze Native/Lua dla KAŻDEJ z nich (Native+Lua CallFunction są strukturalnie gwarantowane przez jedną ścieżkę `ScriptRuntimeHost::RegisterFunction`, więc realną, niezależną częścią do zweryfikowania było wyłącznie wiązanie Visual Graph — i to jest teraz pokryte dla całego katalogu, nie tylko pojedynczych funkcji jak wcześniej). ZABLOKOWANE: część "każda funkcja ma OPIS" — dziś żaden typ (`ScriptApiCatalogFunction`, `LibraryFunctionDesc`) nie ma pola opisu tekstowego; dodanie go retroaktywnie do dziesiątek istniejących funkcji to osobne zadanie dokumentacyjne (naturalnie powiązane z LIB-248 "generator Markdown/reference z manifestu"), nie chcę fabrykować pustych/fałszywych opisów żeby "domknąć" checkbox. Patrz `others/_temp.md` POWRÓT.
- [x] LIB-030 Dodać test, że nieeksponowana funkcja nie jest możliwa do wywołania po nazwie. Dowód: `sources/engine/tests/ScriptRuntimeTests.cpp::RunUnexposedFunctionCannotBeCalledTest` — nazwa nigdy niezarejestrowana wywołana przez `ScriptFunctionRegistry::Call` (jedyny punkt przez który przechodzą Native, Lua `CallFunction` i Visual Graph `CallNative`) zwraca jawny błąd "is not registered", `executed=false`, nigdy ciche powodzenie. Build+testy zielone.

### 4.2 Własność, bezpieczeństwo i błędy

- [x] LIB-031 Zdefiniować własność obiektów tworzących zasoby (`Owned`, `Borrowed`, `Shared`, `Weak`). Dowód: `sources/engine/include/engine/library/EngineLibraryOwnership.hpp`/`.cpp` — `LibraryOwnership{Owned,Borrowed,Shared,Weak}` + `LibraryOwnershipTraits<T>` (specjalizacje kompilacyjne, nie proza — nieklasyfikowany typ to błąd kompilacji, nie cichy domyślny wybór). `EntityHandle` (LIB-008) sklasyfikowany jako `Borrowed` (Scene jest właścicielem), `AssetRef<T>`/`SceneRef` (LIB-009) jako `Shared` (ref-counted `shared_ptr` w `AssetHandle<T>`). Test: `EngineLibraryTests.cpp::RunOwnershipTest` (`static_assert` dla obu typów + `ToString`). Build+testy zielone.
- [x] LIB-032 Zabronić przekazywania wskaźników/referencji C++ poza granicę klatki skryptowej. Dowód: `kb::script::ScriptValue` — jedyny kanał, którym dane przechodzą przez granicę Lua/Visual Graph (argumenty funkcji, wyjścia, payloady zdarzeń, zmienne exposed) — dostał `static_assert` wprost w `ScriptValue.hpp`, że żadna alternatywa `Storage` nie jest wskaźnikiem/referencją; edycja typu dodająca wskaźnik nie skompiluje się. Test: `EngineLibraryTests.cpp::RunNoPointersCrossScriptBoundaryTest` — dodatkowo blokuje dokładny kształt/kolejność `Storage` (6 alternatyw) jako strażnika regresji. `LibraryContextBase::Raw()` (LIB-007) pozostaje jedynym udokumentowanym wyjątkiem (C++-only escape hatch, niebezpieczny do zachowania poza wywołaniem — nigdy nie przechodzi do Lua/Visual Graph). Build+testy zielone (`kb_engine_tests.exe` + `kb_cli_tests.exe`, oba dotknięte zmianą w `ScriptValue.hpp`).
- [x] LIB-033 Dodać kontrolę przynależności `EntityHandle` do świata i sceny. Dowód: zaimplementowane jako część LIB-008 — `EntityHandle::IsAlive(scene)`/`Validate(scene, op)` porównują `sceneId_` (przechwycone przy tworzeniu handle) z `scene.Id()` PRZED sprawdzeniem żywotności encji; niezgodność = `false`/`std::out_of_range` ("belongs to a different world"). Test: `EngineLibraryTests.cpp::RunEntityHandleTest` (`wrongWorldHandle` — handle z jednej sceny sprawdzany względem innej).
- [x] LIB-034 Dodać ochronę przed użyciem entity po `Destroy` i po zmianie generacji. Dowód: zaimplementowane jako część LIB-008 — `EntityHandle::IsAlive(scene)` deleguje do `scene.Entities().IsAlive()` (generation-checked przez flecs, LIB-005), więc handle do zniszczonej encji — nawet jeśli jej indeks zostanie zrecyklowany przez nową encję — zawsze zwraca `false`. Test: `EngineLibraryTests.cpp::RunEntityHandleTest` (zniszczenie encji → `IsAlive`==false + `Validate` rzuca `std::out_of_range`; utworzenie nowej encji po zniszczeniu → stary handle pozostaje `false`, nowa encja żyje).
- [x] LIB-035 Zdefiniować kody błędów: invalid handle, inactive world, unavailable capability, permission, invalid argument, timeout. Dowód: `sources/engine/include/engine/library/EngineLibraryError.hpp` — `LibraryErrorCode{InvalidHandle,InactiveWorld,UnavailableCapability,Permission,InvalidArgument,Timeout}` (dokładnie 6 kategorii z planu, z komentarzem definiującym granice każdej), `ScriptError` (LIB-010) dostało pole `code`. Realne użycie: `EntityHandle::CheckError(scene, op)` (`EngineLibraryEntityHandle.hpp`/`.cpp`) — nierzucający odpowiednik `Validate()`, zwraca `std::optional<ScriptError>` z `code=InvalidHandle` dla wszystkich 3 trybów awarii uchwytu (invalid/wrong-world/stale); wspólna logika wydzielona do `FirstFailureReason()`, żeby `Validate()` i `CheckError()` nie duplikowały trzech sprawdzeń. Test: `EngineLibraryTests.cpp::RunErrorCodeTest`. Build+testy zielone.
- [x] LIB-036 Dodać strukturę błędu zawierającą moduł, funkcję, entity, asset i fazę lifecycle. Decyzja: rozszerzyć istniejący `kb::script::ScriptDiagnostic` (już miał entity/asset/backend, faktycznie przepływa przez cały runtime — `ScriptBackendExecutionResult`/`ScriptRuntimeExecutionResult`), nie tworzyć równoległego typu w `kb::library`. Dowód: `sources/engine/include/engine/script/ScriptBackend.hpp` — dodane pola `module`, `function`, `lifecyclePhase` (`std::optional<ScriptLifecycleEvent>`, bo nie każda diagnostyka ma fazę). Wypełnianie przyrostowe (jak LIB-017/029): `NativeScriptBackend::InvokeNativeCallback` (naprawa z LIB-010) teraz realnie wypełnia `lifecyclePhase` z `context.Lifecycle()`. Test: rozszerzony `ScriptRuntimeTests.cpp::RunNativeScriptBackendExceptionSafetyTest` (weryfikuje `lifecyclePhase == Tick` na prawdziwej diagnostyce z wyjątku). Build+testy zielone (`kb_engine_tests.exe` + `kb_cli_tests.exe`).
- [x] LIB-037 Ujednolicić limity wejścia: długość stringów, kolekcji, payloadów zdarzeń i rekurencji graphów. Dowód: `sources/engine/include/engine/library/EngineLibraryInputLimits.hpp` — `LibraryInputLimits{maxStringLength=65536, maxCollectionSize=4096, maxEventPayloadArguments=32, maxGraphRecursionDepth=8}`. Realnie egzekwowane DWA z czterech: (1) `maxStringLength` — nowy limit w `kb::script::ScriptFunctionRegistry::ValidateInputs` (`ScriptFunctionRegistry.cpp`, jedyny punkt walidacji dla Native+Lua+Visual Graph), zweryfikowany testem z prawdziwym stringiem 65536B (OK) i 65537B (odrzucone); (2) `maxGraphRecursionDepth` — JUŻ istniał i był egzekwowany jako `ScriptRuntimeDispatchOptions::maxEventDepth` (domyślnie 8), teraz udokumentowany jako spełniający to kryterium, wartości zweryfikowane jako identyczne testem. `maxCollectionSize`/`maxEventPayloadArguments` to świadomie zarezerwowane wartości polityki — brak dziś typu kolekcji (`Array<T>` to LIB-058) i mechanizmu walidacji payloadu zdarzeń bez przebudowy `Emit`/`EmitTo` (dziś `void`, brak kanału błędu — dodanie cichego odrzucania złamałoby zakaz cichych fallbacków) do egzekwowania; nie fabrykowano fałszywej walidacji. Test: `EngineLibraryTests.cpp::RunInputLimitsTest`. Build+testy zielone.
- [x] LIB-038 Dodać ochronę przed reentrancy w zdarzeniach i callbackach. Dowód: `kb::script::ScriptFunctionRegistry` — jedyny choke point przez który przechodzą wszystkie wywołania Native/Lua/Visual Graph — dostał licznik głębokości `mutable std::size_t callDepth_` (`ScriptFunctionRegistry.hpp`) i strażnika `kMaxCallDepth=64` (`ScriptFunctionRegistry.cpp`, anonimowa przestrzeń nazw obok `kMaxScriptStringArgumentLength` z LIB-037). `Call()` odrzuca wywołanie z jasnym diagnostykiem ("exceeded the maximum call depth... reentrant or mutually recursive call chain") ZANIM wejdzie w callback, gdy `callDepth_ >= kMaxCallDepth`; poza tym RAII `CallDepthGuard` (lokalna klasa w anonimowej przestrzeni nazw) inkrementuje/dekrementuje licznik dokładnie wokół `iter->callback(...)`, więc głębokość jest poprawnie przywracana na każdej ścieżce wyjścia (normalny powrót, oba catch z LIB-010). Chroni to zarówno bezpośrednią rekurencję (funkcja wywołuje samą siebie), jak i łańcuch wzajemnie rekurencyjnych funkcji, bo licznik jest globalny dla rejestru, nie per-funkcja. Test: `ScriptRuntimeTests.cpp::RunScriptFunctionRegistryReentrancyGuardTest` — rejestruje `Tests.Recurse`, którego callback woła `host.Functions().Call("Tests.Recurse", ...)` na sobie; weryfikuje że łańcuch zatrzymuje się na limicie głębokości z jasnym komunikatem zamiast przepełnić stos, że kolejne wywołanie tego samego łańcucha deterministycznie zawodzi tak samo, i że sam rejestr pozostaje nienaruszony (sygnatura nadal znaleziona) po odrzuceniu. Build+testy zielone: `kb_engine_tests.exe` (cały `RunAllSuites`, exit 0, brak wyjątków) oraz `kb_cli_tests.exe` (exit 0) po pełnym `cmake --build . --target kb_engine_tests/kb_cli_tests --config Debug`.
- [ ] LIB-039 ZABLOKOWANE. Dodać jawne anulowanie `Subscription`, `TimerHandle`, `AsyncHandle` i `TaskHandle`. Powód blokady: żaden z czterech typów jeszcze nie istnieje w kodzie (zweryfikowane grep po `sources/engine/include`+`src`: zero wystąpień `Subscription`/`TimerHandle`/`AsyncHandle`/`TaskHandle`/`Coroutine`/`SetTimeout`/`TaskScheduler`, brak publicznego `Subscribe(`). Dzisiejsze dostarczanie zdarzeń to wyłącznie wewnętrzny dispatch lifecycle (`ScriptRuntime::DispatchEvent` + kolejka pending) bez uchwytu widocznego dla użytkownika — nie ma czego anulować. Każdy typ ma docelowe zadanie tworzące go: `Subscription` → LIB-105 (sekcja 8, `Events.Subscribe/Unsubscribe/Emit/EmitDeferred/Broadcast`), `TimerHandle` → LIB-095 (sekcja 7, `Timer.Once/Repeat/Cancel/Pause/Resume`), `AsyncHandle`/`TaskHandle` → LIB-097..100 (sekcja 7, wybór modelu Coroutine/Task + `AsyncResult<T>`). Implementacja LIB-039 przed tymi zadaniami oznaczałaby wymyślenie czterech API bazowych pod szyldem ticketu opisanego tylko jako "dodać anulowanie" — duplikacja/uprzedzenie dedykowanych zadań backlogu.
- [ ] LIB-040 ZABLOKOWANE (ta sama przyczyna co LIB-039). Przetestować usuwanie entity z callbacku, callback po anulowaniu i błąd po unloadzie sceny. Test wymaga istniejących uchwytów z LIB-039, więc czeka na te same zależności (LIB-095, LIB-097..100, LIB-105).

## 5. Typy podstawowe, matematyka i dane

Matematyka musi odwzorowywać typy silnika, nie tworzyć równoległego zestawu
wektorów. Typy wartościowe są wspólne dla native, Lua serializer'a i pinów
Visual Graph.

- [x] LIB-041 Udostępnić `Bool`, `Int32`, `Int64`, `UInt32`, `Float`, `Double`, `String`, `Name`, `Guid` i `Hash`. Decyzja zakresu: `Bool`/`Float`/`String` już istniały; `Int` już spełnia "Int32" (natywny `int` jest 32-bitowy na każdej platformie docelowej silnika) — nie dodano osobnego enumeratora Int32, żeby uniknąć czystego przemianowania w każdym istniejącym miejscu użycia `Int`. Dodano 6 nowych: `Int64`, `UInt32`, `Double`, `Name`, `Guid`, `Hash` — dołożone NA KOŃCU `ScriptValueType`/`VisualGraphValueType` (nie wstawione pomiędzy istniejące), żeby liczbowa wartość żadnego istniejącego enumeratora się nie zmieniła (ważne dla `kb::library::DescribeType`'s tablicy indeksowanej rzutowaniem enum→size_t). Dowód (pełny łańcuch Native/Lua/Visual Graph, nie tylko definicja typu): `ScriptValue`/`VisualGraphRuntimeValue` (`Storage` rozszerzone o `int64_t`/`double`; `UInt32`/`Hash` dzielą istniejący `uint64_t`, `Name`/`Guid` dzielą istniejący `std::string` — oznaczone przez `type_`, ten sam wzorzec co już istniejący dla `Entity`/`Component`) z nowymi konstruktorami/akcesorami (`AsInt64`, `AsUInt32`, `AsDouble`); wszystkie 19 exhaustive `switch` nad `ScriptValueType`/`VisualGraphValueType` w całym `sources/engine` zaktualizowane (m.in. `ToString`, `ToVisualGraphValueType`, `ScriptFunctionVisualGraphBindings`, `ScriptSceneVisualGraphBindings`, `ScriptSharedVisualGraphBindings`, `ScriptApiNameCollector`, `VisualGraphScriptBackend`, `PucLuaValueBridge::Push/DefaultFor`, `ScriptApiExport::LuaTypeName`, `ScriptAssetLoader::TryParseScriptValue/DefaultScriptValue`, `VisualGraphNativeCodeGenerator` — 4 switche + kontrakt wygenerowanej klasy `VisualGraphNativeExecutionContext`/`VisualGraphNativeFunctionResult` rozszerzony o `Read/Store<Typ>` dla wszystkich 6); 2 nie-exhaustive if-chainy (`ScriptAssetLoader::TryParseScriptValueType`, `ScriptApiDeclarationParser::TryParseType`) też rozszerzone; `kb::library::EngineLibraryTypeDesc` (`kTypeCount=13`, 6 nowych deskryptorów). Świadomie POZA zakresem (udokumentowane, nie ciche): sugar `Self.GetProperty<Typ>`/`Shared.Get<Typ>` (tablice `kPropertyBindingTypes`/`kSharedBindingTypes` sterowane ręcznie, NIE iterowane po enumie — nie wymagają aktualizacji do kompilacji) dla nowych 6 typów nie dodany, bo żaden komponent nie ma jeszcze pola tego typu (LIB-076, rejestr komponentów, jeszcze nie istnieje) ani żadna zmienna Shared nie jest jeszcze typowana na te typy w praktyce — dodanie samej sygnatury bez realnego konsumenta byłoby fasadą. Walidacja formatu Guid i przyszłe interning Name świadomie odłożone do LIB-063 (parsing GUID/liczb) — ta pozycja definiuje TYP wartości, nie jego API parsowania. Test: `EngineLibraryTests.cpp::RunExpandedValueTypesTest` — konstrukcja+akcesory dla wszystkich 6 typów, structural equality odróżniająca typy dzielące storage (Hash≠Entity mimo wspólnego uint64_t, Name≠Guid mimo wspólnego string), round-trip przez `ToVisualGraphValue()`, `ToString` dla wszystkich 6, oraz PRAWDZIWE wywołanie funkcji przez `ScriptFunctionRegistry::Call` z pinem typu `Hash` (`Tests.EchoHash`) dowodzące że `ValidateInputs`/`ValidateOutputs` faktycznie akceptują nowy typ, nie tylko że się kompiluje. `RunTypeDescTest`'s `kAllTypes` i LIB-032's `RunNoPointersCrossScriptBoundaryTest` (Storage teraz 8 alternatyw) też rozszerzone. Build+testy zielone: pełny `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wszystkie testy, zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe` (oba exit 0, brak asercji).
- [x] LIB-042 Udostępnić `Vec2`, `Vec3`, `Vec4`, `IVec2`, `Color`, `Rect`, `Bounds`, `Ray`, `Plane` i `Pose`. Decyzja architektoniczna (zbadana przed implementacją): `kb::script::ScriptValue`/`kb::visual::VisualGraphValueType` są CELOWO czysto skalarne (LIB-032's `static_assert` blokuje wskaźniki/typy złożone w `Storage`), a KAŻDE istniejące API skryptowe (ScriptTransformApi, ScriptWorldApi, ScriptPhysicsApi) już dekomponuje wektory na osobne, nazwane skalarne piny Float (`x`/`y`/`z`, `originX`/`originY`/`originZ`, ...) — LIB-042 formalizuje ten wzorzec jako sankcjonowaną konwencję Native/Lua/Visual Graph zamiast dodawać złożony typ pinu (co złamałoby strażnika LIB-032 i wymagałoby przebudowy całego kontraktu marshallingu). Dowód: nowy moduł `kb::math` (`sources/engine/include/engine/math/EngineMath.hpp` + `src/math/EngineMath.cpp`) — jedyna kanoniczna rodzina typów (Vec2/Vec3/Vec4/IVec2/Quat/Color/Rect/Bounds/Ray/Plane/Pose) z udokumentowanymi konwencjami (Bounds=center+half-extents jak już istniejący `ColliderComponent`, Ray.direction/Plane.normal oczekiwane znormalizowane, Color domyślnie opaque white=identity dla mnożenia, Pose=position+rotation bez skali). `kb::scene::Vec3`/`Quat` (`TransformComponent.hpp`) są teraz ALIASAMI (`using Vec3 = kb::math::Vec3`) do tej samej definicji — zero zmian w żadnym z ~50+ istniejących miejsc użycia. Realnie wyeliminowano 5 PRYWATNYCH duplikatów matematyki Vec3/Quat, które faktycznie istniały w kodzie (dokładnie ten "równoległy zestaw wektorów", przed którym ostrzega nagłówek tej sekcji) — odkryte dopiero przez błędy kompilacji (niejednoznaczność ADL) po aliasowaniu: `ScriptPhysicsApi.cpp` (Vec3+Add/Dot/Length/Normalize/Abs/Max), `sources/renderer/src/scene/EcsRenderTransformResolver.cpp` (Add/Dot), `sources/editor/src/scene/EditorViewportCameraState.cpp` (Add/Length/Normalize), `sources/editor/src/scene/transform_edit/EditorSceneTransformEditController.cpp` (Add), `sources/plugins/audio_miniaudio/scene/MiniaudioListenerSynchronizer.cpp` (Add) — każdy zastąpiony wywołaniami `kb::math::*`/`operator+`, zachowując identyczną matematykę (zweryfikowane testami fizyki/transformacji, zero regresji). Świadomie POZA zakresem: 3 pliki z WŁASNYM prywatnym `struct Vec3` niepowiązanym z `kb::scene::Vec3` (mesh-import scratch types w `RenderMeshAssetFinalizer.cpp`, `EditorMaterialPreviewMeshFactory.cpp`, `EditorMeshPreviewRasterizer.cpp`) — nie kolidują (inny typ, ADL ich nie dotyka) i konsolidacja byłaby przebudową importu siatek poza zakresem tego zadania. Test: nowy `sources/engine/tests/EngineMathTests.cpp` (`RunEngineMathTests`, zarejestrowany jako suite `engine-math`) — tożsamość aliasu `kb::scene::Vec3`==`kb::math::Vec3` (static_assert), konwencje domyślnych wartości dla wszystkich 10 typów, poprawność matematyczna Vec3 (Dot/Length/Normalize/Abs/Max/operatory na znanych wartościach, w tym trójkąt 3-4-5 i ochrona przed dzieleniem przez zero w Normalize). Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wtyczki, zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe`+`kb_renderer_tests.exe`+`kb_editor_tests.exe` (wszystkie exit 0) — pełny zakres bo zmiana (alias + eliminacja duplikatów) dotknęła kodu poza `sources/engine`.
- [x] LIB-043 Udostępnić `Quat`, `Mat3`, `Mat4` oraz jawną konwencję handedness i kolejność mnożenia. Dowód: `Quat` już istniał (LIB-042); dodano `kb::math::Mat3`/`Mat4` (`sources/engine/include/engine/math/EngineMath.hpp`) — column-major, identyczny layout co już istniejący `kb::scene::WorldTransformAffine3x4` i `bx::mtx*` (matematyka renderera z bgfx/bx — zweryfikowano że KAŻDE istniejące wywołanie `bx::mtxLookAt/mtxProj/mtxOrtho` pomija argument `_handedness`, więc silnik już ukrycie używał domyślnego `bx::Handedness::Left`). Konwencja handedness/mnożenia udokumentowana WPROST komentarzem przy `Quat` (left-handed, Y-up, dodatnia rotacja wg reguły lewej dłoni) — po raz pierwszy w całym kodzie jako jawna decyzja, nie domyślny efekt pominiętego parametru. Dodano operacje: `operator*(Quat,Quat)` (iloczyn Hamiltona, kolejność `lhs*rhs` = najpierw `rhs` potem `lhs`, zgodne z już istniejącą konwencją `parent*child` w `EcsRenderTransformResolver::Compose`), `Normalize(Quat)`, `Cross(Vec3,Vec3)`, `Rotate(Quat,Vec3)` (ogólny wzór Rodriguesa, poprawny dla dowolnego kwaternionu nie tylko znormalizowanego), `operator*(Mat4,Vec4)`, `operator*(Mat4,Mat4)`, `FromTRS(translation,rotation,scale)` (budowana przez `Rotate()` — jedno źródło prawdy zamiast niezależnej transkrypcji wzoru kwaternion→macierz). Dodanie `Cross`/`Normalize(Quat)`/`Rotate`/`operator*(Quat,Quat)`-równoważnego `Multiply` do kb::math ujawniło (przez błędy kompilacji ambiguous-ADL) KOLEJNE 4 prawdziwe prywatne duplikaty tej samej matematyki, nienaprawione jeszcze w LIB-042: `sources/engine/src/scene/SceneRuntime.cpp` (Normalize), `sources/renderer/src/scene/EcsRenderTransformResolver.cpp` (Cross, Normalize, Multiply→teraz operator*, Rotate — pełna konsolidacja), `sources/plugins/audio_miniaudio/scene/MiniaudioListenerSynchronizer.cpp` (Cross, Rotate), `sources/editor/src/scene/EditorViewportCameraState.cpp` (Cross), `sources/editor/src/app/scene_viewport/EditorSceneViewportMeshPicker.cpp` (Normalize, Rotate) — wszystkie zamienione na `kb::math::*`, zero regresji (physics/transform/audio-listener testy nadal zielone). Test: rozszerzony `EngineMathTests.cpp::RunQuatAndMatrixMathTest` — Cross na wektorach bazowych, tożsamość lewa/prawa mnożenia kwaternionów, ZNANA wartość (rotacja +X o 90° wokół +Z daje +Y, przypinająca konwencję handedness konkretną liczbą, nie tylko "kompiluje się"), zachowanie długości wektora przy Rotate, Normalize (w tym ochrona przed dzieleniem przez zero), Mat3/Mat4 identity, Mat4·Vec4, `FromTRS` (translacja i skala niezależnie zweryfikowane), Mat4·Mat4 z tożsamością. Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wtyczki, zero błędów, wymagało 2 rund poprawek po odkryciu kolejnych duplikatów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe`+`kb_renderer_tests.exe`+`kb_editor_tests.exe` (wszystkie exit 0).
- [x] LIB-044 Udostępnić `Radians`, `Degrees`, bezpieczne konwersje i nie mieszać jednostek w API. Dowód: `kb::math::Radians`/`Degrees` (`EngineMath.hpp`) — silnie typowane klasy-wrappery (nie `float` z sufiksem w nazwie zmiennej, wzorzec który wcześniej istniał jako 6+ NIEZALEŻNIE reimplementowanych helperów `DegreesToRadians` w `sources/renderer`+`sources/editor`, każdy z osobna przepisujący `degrees*pi/180`). Konstruktor z `float` jest `explicit` (żaden call site nie może przypadkiem pomieszać jednostek przez niejawną konwersję), a MIĘDZY `Radians`/`Degrees` NIE MA niejawnej konwersji w ogóle — zamiana wymaga jawnego `ToRadians`/`ToDegrees`, więc "która to jednostka" nigdy nie jest ciche. Dodano `kb::math::kPi` (jedno źródło prawdy zamiast literału `3.14159...` powielonego w ~8 miejscach) oraz operatory tego samego typu (`+`, `-`, `*float`, `==`, `!=`, `<`) do akumulacji/porównań (np. yaw/pitch kamery, clamp FOV) bez utraty bezpieczeństwa jednostek. Realnie skonsolidowano WSZYSTKIE 6 zidentyfikowanych duplikatów helpera `DegreesToRadians` (`RenderSceneProxyConverters.cpp`, `SceneLightingPacker.cpp`, `SceneForwardLightSelector.cpp`, `EditorViewportCameraState.cpp`, `ScenePanelContentRenderer.cpp`, `EditorSceneViewportMath.cpp`/`EditorSceneViewportMath::DegreesToRadians`) — każdy teraz deleguje do `kb::math::ToRadians`, ta sama formuła, zero zmiany zachowania. Świadomie POZA zakresem: pola komponentów (`CameraComponent::verticalFovDegrees`, `LightComponent::innerConeDegrees/outerConeDegrees`) i property w `ScriptSceneComponentApi.cpp` NIE zostały przepisane z `float` na `Degrees` — zmiana typu pola dotknęłaby serializacji assetów (`SceneAssetCameraComponentCodec`) i rejestru property w ECS, czyli osobne, większe i ryzykowniejsze zadanie niż "udostępnić typ"; podobnie pojedyncze inline'owane wyrażenia `*pi/180` bez nazwanego helpera (np. `EditorSceneViewportMeshPicker.cpp:307`, `SceneGizmoPass.cpp`) pozostawione — LIB-044 skonsolidował nazwane, powielone FUNKCJE konwersji, nie każde pojedyncze wystąpienie arytmetyki z pi w całym kodzie. Test: `EngineMathTests.cpp::RunAngleUnitsTest` — znane wartości konwersji (180°=π rad), round-trip degrees→radians→degrees, `static_assert` blokujący niejawną konwersję float→Radians/Degrees i Radians↔Degrees (kompilacyjny dowód braku cichego mieszania jednostek, nie tylko runtime), operatory arytmetyczne/porównania. Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wtyczki, zero błędów) oraz `kb_engine_tests.exe`+`kb_renderer_tests.exe`+`kb_editor_tests.exe`+`kb_cli_tests.exe` (wszystkie exit 0).
- [x] LIB-045 Udostępnić `Math.Clamp`, `Lerp`, `InverseLerp`, `Remap`, `SmoothStep`, `MoveTowards`, `Damp`. Decyzja zakresu: prefiks "Math." oznacza REALNĄ rejestrację jako funkcje ScriptFunctionRegistry (Native/Lua/Visual Graph callable), nie tylko natywne C++ helpery — potwierdzone brakiem jakiegokolwiek "Math." w katalogu przed tym zadaniem. Dowód: natywna implementacja w `kb::math` (`EngineMath.hpp`/`.cpp`) — `Clamp`, `Lerp`/`InverseLerp`/`Remap` (zbudowane jedno na drugim, jedno źródło prawdy dla clampowania), `SmoothStep` (kubiczny 3t²-2t³), `MoveTowards` (bez przestrzelenia celu), `Damp` (critically-damped spring, formuła Game Programming Gems 4 / Unity `Mathf.SmoothDamp` — jedyna ugruntowana semantyka "Damp" w silnikach gier, nic w planie jej nie doprecyzowywało, więc to świadoma decyzja inżynierska udokumentowana wprost w kodzie; zwraca `{value, velocity}` zamiast przyjmować `velocity` przez referencję, bo LIB-032 zakazuje referencji/wskaźników przez granicę skryptu). Nowy moduł `sources/engine/src/script/ScriptMathApi.cpp`/`.hpp` rejestruje wszystkie 7 jako `Math.*` przez ten sam wzorzec co `ScriptWorldApi`/`ScriptTimeApi` (lokalny helper `RegisterFunction`, `host.RegisterFunction`), wpięty do `EngineLibraryModule::Catalog()` jako 7. moduł ("Math", `ownerRuntime` = "kb::math (stateless...)" — jedyny moduł bez zaplecza runtime, bo to czyste funkcje). ŚWIADOMIE brak dedykowanego Lua "sugar" (np. bezpośrednie `Math.Clamp(...)` w Lua) — funkcje są już w pełni osiągalne z Lua przez generyczny `CallFunction("Math.Clamp", ...)` (ten sam mechanizm co `World.*`/`Physics.*` przed dodaniem ich sugar-wrapperów), a dodawanie 7 kolejnych ręcznych Lua-C funkcji tuż przed planowaną konsolidacją Lua sugar (LIB-242, `kLuaBindings`+`PucLuaFunctionApi::Attach`) byłoby pracą do powtórzenia. Zaktualizowano 3 testy z twardo zakodowaną listą 6 modułów (`RunModuleInstallCoversAllDomainsTest`, `RunModuleInstallReportsDuplicateDiagnosticsTest` diagnostics.size()==6→7, `RunModuleCatalogTest` expectedNames) na 7. Test: `EngineMathTests.cpp::RunScalarMathFunctionsTest` (znane wartości + edge case'y: a==b w InverseLerp, cel już osiągnięty w MoveTowards, smoothTime<=0 w Damp bez NaN) + `ScriptRuntimeTests.cpp::RunScriptMathApiTest` (PRAWDZIWE wywołania wszystkich 7 przez `ScriptFunctionRegistry::Call`, nie tylko sprawdzenie rejestracji) + generyczny `RunCatalogFunctionsHaveVisualGraphBindingsTest` automatycznie objął nowe funkcje (bez zmian, bo iteruje cały katalog). Build+testy zielone: PEŁNY `cmake --build . --config Debug` (zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe` (oba exit 0).
- [x] LIB-046 Udostępnić `Min`, `Max`, `Abs`, `Sign`, `Floor`, `Ceil`, `Round`, `Frac`, `Mod`, `Sqrt`, `Pow`, `Exp`, `Log`. Dowód: natywne skalarne przeciążenia w `kb::math` (`EngineMath.hpp`/`.cpp`) — `Min`/`Max`/`Abs` przeciążają już istniejące wersje Vec3 (bez konfliktu, różne typy argumentów, i BEZ ryzyka ambiguous-ADL jak przy Vec3/Quat w LIB-042/043, bo `float` jest typem wbudowanym bez powiązanej przestrzeni nazw — ADL nigdy nie sprawdza `kb::math` dla czysto skalarnych wywołań; zweryfikowano pełnym buildem, zero nowych kolizji). Udokumentowane jawnie 2 nieoczywiste konwencje: `Frac`/`Mod` są floor-based (jak GLSL `mod`, nie C `fmod`) — `Mod(-1,4)=3`, nie `-1` — bo to jedyna konwencja dająca dobrze zdefiniowane zawijanie (np. kąta) dla ujemnej dywidendy; `Log` to logarytm naturalny (nie log10/log2), jawnie nazwane w komentarzu bo "Log" samo w sobie jest niejednoznaczne. Zarejestrowano wszystkie 13 jako `Math.*` w TYM SAMYM `ScriptMathApi.cpp`/module "Math" z LIB-045 (nie nowy moduł — kontynuacja tego samego wzorca). Test: `EngineMathTests.cpp::RunScalarMathFunctions2Test` (znane wartości + edge case'y: `Sign(0)=0` nie ±1, `Frac`/`Mod` dla ujemnych argumentów, `Round` zaokrągla w kierunku od zera przy .5, `Log(e)=1` potwierdzające naturalny logarytm) + rozszerzony `ScriptRuntimeTests.cpp::RunScriptMathApiTest` (rejestracja wszystkich 19 funkcji Math.* razem + PRAWDZIWE wywołania Math.Max/Math.Mod/Math.Sqrt przez `ScriptFunctionRegistry::Call`). Build+testy zielone: PEŁNY `cmake --build . --config Debug` (zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe` (oba exit 0).
- [x] LIB-047 Udostępnić funkcje trygonometryczne i odwrotne ze zdefiniowaną domeną błędu. Decyzja zakresu: `Sin`/`Cos`/`Tan`/`Atan` przyjmują dowolną rzeczywistą wartość (domena nieograniczona), ale `Asin`/`Acos` mają PRAWDZIWĄ ograniczoną domenę wejścia ([-1,1]) — to właśnie tu "zdefiniowana domena błędu" ma realne znaczenie. Dowód natywny: `kb::math::Sin/Cos/Tan/Atan` przyjmują/`Asin/Acos/Atan/Atan2` zwracają `kb::math::Radians` (nie goły float — LIB-044's typowane jednostki kątowe, trygonometria to najbardziej naturalne miejsce na wymuszenie tej konwencji, bo pomylenie stopni/radianów w sin/cos to klasyczny błąd). Na poziomie natywnym `Asin`/`Acos` po prostu delegują do `std::asin`/`std::acos` (standardowy kontrakt IEEE-754 NaN dla wejścia poza domeną — to NIE jest cichy fallback, to udokumentowane zachowanie które każdy natywny caller C++ już rozumie). Na granicy skryptu (`ScriptMathApi.cpp`) `Math.Asin`/`Math.Acos` WALIDUJĄ domenę i zwracają PRAWDZIWY błąd (`ScriptFunctionCallResult{.executed=false, .errors={...}}`) dla `|value|>1` — zamiast pozwolić NaN cicho popłynąć przez graf skryptowy aż trafi np. do `Transform.SetRotation` wiele węzłów później bez wskazania źródła. To jedyne funkcje Math.* (poza Damp) z realną walidacją błędu, reszta (045/046) zawsze się wykonuje. `Atan2(y,x)` rozwiązuje ćwiartkę z obu znaków (czego sam `Atan(y/x)` nie potrafi — nie odróżni (1,1) od (-1,-1)); `Atan2(0,0)` konwencjonalnie zwraca 0. Zarejestrowano wszystkie 7 w tym samym module "Math". Test: `EngineMathTests.cpp::RunTrigFunctionsTest` (znane wartości, w tym rozróżnienie Atan2 ćwiartek) + rozszerzony `ScriptRuntimeTests.cpp::RunScriptMathApiTest` (26 nazw + PRAWDZIWE wywołania Math.Sin/Math.Atan2 sukces + Math.Asin/Math.Acos z wartością poza domeną → realny błąd, nie NaN + Math.Asin z wartością brzegową 1.0 → sukces, nie fałszywe odrzucenie). Build+testy zielone: PEŁNY `cmake --build . --config Debug` (zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe` (oba exit 0).
- [x] LIB-048 Udostępnić `Dot`, `Cross`, `Length`, `Normalize`, `Distance`, `Project`, `Reflect`, `Refract`. Decyzja zakresu: PIERWSZE Math.* operujące na Vec3 zamiast czystych skalarów — `Dot`/`Cross`/`Length`/`Normalize` już istniały natywnie w kb::math (LIB-042), tu dodane realnie 4 nowe (`Distance`, `Project`, `Reflect`, `Refract`) + rejestracja WSZYSTKICH 8 jako Math.*. Konwencja marshallingu: Vec3 NIE jest typem pinu skryptowego (LIB-032/LIB-042 — ScriptValue czysto skalarny), więc każda funkcja Vec3-kształtna dekomponuje się na piny `<prefix>X`/`<prefix>Y`/`<prefix>Z`, dokładnie ten sam wzorzec co już istniejący `Physics.Raycast` (originX/originY/originZ) — sformalizowany przez nowe helpery `Vec3Arg`/`Vec3Result`/`Vec3Pins`/`ConcatPins` w `ScriptMathApi.cpp`, nie wynaleziony od nowa. Dowód natywny: `Distance(a,b)=Length(a-b)`; `Project(value,onto)` — rzut wektorowy (nie skalarny), zwraca wektor zerowy dla `onto`=0 (ten sam strażnik co `Normalize`, bez dzielenia przez zero); `Reflect(incident,normal)` — standardowa formuła GLSL `reflect`, `normal` oczekiwany znormalizowany (ta sama konwencja co `kb::math::Plane.normal`); `Refract(incident,normal,eta)` — standardowa formuła GLSL `refract`, zwraca wektor zerowy przy całkowitym wewnętrznym odbiciu (k<0) zamiast NaN z `sqrt` ujemnej liczby — to LEGALNY wynik fizyczny, nie błędne wejście do odrzucenia (kontrast z LIB-047's Asin/Acos, gdzie wyjście poza domeną JEST błędem). Test: `EngineMathTests.cpp::RunVec3DistanceProjectReflectRefractTest` (trójkąt 3-4-5 dla Distance, rzut na oś X, odbicie od podłogi 180°, przejście bez załamania przy eta=1, całkowite wewnętrzne odbicie→wektor zerowy) + rozszerzony `ScriptRuntimeTests.cpp::RunScriptMathApiTest` (34 nazwy + PRAWDZIWE wywołania Math.Cross i Math.Distance przez zdekomponowane piny aX/aY/aZ/bX/bY/bZ). Build+testy zielone: PEŁNY `cmake --build . --config Debug` (zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe` (oba exit 0).
- [x] LIB-049 Udostępnić `Angle`, `SignedAngle`, `Slerp`, `LookRotation`, `FromToRotation`, `RotateTowards`. Dowód: `Angle`/`SignedAngle` operują na Vec3 i zwracają `kb::math::Radians` (konwencja z LIB-047 — funkcja zwracająca kąt zwraca typ kąta); `SignedAngle` wymaga referencyjnej osi (jak `Vector3.SignedAngle` w Unity) do rozstrzygnięcia znaku przez `Dot(Cross(a,b),axis)`. `Slerp`/`LookRotation`/`FromToRotation`/`RotateTowards` operują na Quat — dowód poprawności matematycznej NIE przez ręczne przypięcie współrzędnych kwaternionu (podatne na błąd), tylko przez zastosowanie już zweryfikowanego `Rotate()` (LIB-043) do wyniku i sprawdzenie że ląduje na oczekiwanym wektorze (np. `LookRotation(+X,+Y)` zastosowane do lokalnego forward (+Z) musi dać +X) — kompozycja z istniejącą, przetestowaną maszynerią zamiast duplikowania weryfikacji. `LookRotation`: standardowa konwersja macierz rotacji→kwaternion (metoda Shepperda) na bazie ortonormalnej zbudowanej z `forward`/`up` (z degenerate-case fallbackiem gdy forward‖up); `LookRotation(+Z,+Y)` (silnikowa konwencja forward=lokalny +Z, zgodna z domyślnym `Ray.direction`) daje dokładnie identity — zweryfikowane algebraicznie i testem. `FromToRotation`: formuła "najkrótszy łuk" (jak Ogre3D `Vector3::getRotationTo`) z jawną obsługą przypadku dokładnie przeciwnych wektorów (nieskończenie wiele poprawnych osi — wybrana dowolna prostopadła, ta sama rezolucja co Unity/Ogre3D). `RotateTowards`: kąt między rotacjami z `|Dot(from,to)|` (bo q i -q to ta sama rotacja), Quat-odpowiednik `MoveTowards`. Zarejestrowano wszystkie 6 w module "Math" — Quat też NIE jest typem pinu skryptowego, więc dekomponuje się na `<prefix>X/Y/Z/W` (nowe helpery `QuatArg`/`QuatResult`/`QuatPins`/`QuatOutputPins`, ten sam wzorzec co Vec3 z LIB-048). Test: `EngineMathTests.cpp::RunRotationFunctionsTest` (Angle/SignedAngle na wektorach bazowych, Slerp przy t=0/t=1/identycznych kwaternionach bez dzielenia przez zero, LookRotation zweryfikowane przez Rotate(), FromToRotation w tym przypadek dokładnie przeciwnych wektorów, RotateTowards osiąga cel przy dużym maxDelta i nie przeskakuje przy małym) + rozszerzony `ScriptRuntimeTests.cpp::RunScriptMathApiTest` (40 nazw + Math.Angle/Math.LookRotation/Math.Slerp przez zdekomponowane piny). Build+testy zielone: PEŁNY `cmake --build . --config Debug` (zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe` (oba exit 0). Blok wektor/kwaternion/trygonometria (LIB-041..049) sekcji 5 teraz kompletny; sekcja 5 jako całość kontynuuje się w LIB-050 (noise/random) i dalej (Easing, Curve/Gradient, NaN/infinity, testy property/parity, kolekcje i tekst).
- [x] LIB-050 Udostępnić `Perlin`/gradient noise i deterministic seeded random bez globalnego ukrytego generatora. Decyzja zakresu: LIB-050 dostarcza CZYSTE (bezstanowe) funkcje — `Random01(seed,index)` i `Noise1D/2D/3D(pozycja,seed)` — każda deterministyczna funkcja SWOICH JAWNYCH argumentów, bez singletona/globalnego generatora do resetowania czy zaawansowania; pełny stanowy `RandomStream` (seed+snapshot+integer/float/range/shuffle) to osobne, następne zadanie (LIB-051), budujące na tym fundamencie. Dowód: `kb::math::Hash32` (32-bitowy hash z finalizerem miksującym bity, stałe MurmurHash3 fmix32 — nie kryptograficzny, ale deterministyczny i z dobrą dyfuzją) jako jedno wspólne źródło losowości dla `Random01`/`Noise*`. `Noise3D` to prawdziwy gradient noise (algorytm Kena Perlina "improved noise" 2002, 12-kierunkowy wybór gradientu przez `hash&15`, krzywa wygładzająca `Fade` C2-ciągła) — NIE value noise (który plan by odrzucił, bo zadanie explicite mówi "Perlin/gradient"). `Noise1D`/`Noise2D` REUŻYWAJĄ `Noise3D` z nieużywanymi osiami przypiętymi do 0 (jedna implementacja, nie trzy) — zweryfikowane matematycznie że to POPRAWNIE redukuje wymiarowość (nie przybliżenie). Kluczowa właściwość matematyczna gradient noise (zero w każdym całkowitoliczbowym punkcie siatki, dla dowolnego seeda — bo wektor odległości od punktu siatki do siebie samego wynosi zero) zweryfikowana testem. `seed`/`index` to `ScriptValueType::UInt32` (LIB-041), nie Int — seed to nieprzezroczysty identyfikator, nie ciągła wielkość. Zarejestrowano wszystkie 4 w module "Math". Test: `EngineMathTests.cpp::RunNoiseAndRandomTest` — determinizm (te same argumenty→ten sam wynik), różne seedy→różne wyniki (na NIEsymetrycznym punkcie próbki — odkryto i naprawiono błąd testu: punkt idealnie symetryczny (0.5,0.5,0.5) ma tylko 3 możliwe wartości Grad niezależnie od hasha, co dawało fałszywe alarmy "różne seed dają ten sam wynik" — to była wada testu, nie algorytmu), zero w punktach siatki (w tym origin), niezerowość poza siatką, Noise1D/2D dokładnie zgodne z Noise3D przy przypiętych osiach. Rozszerzony `ScriptRuntimeTests.cpp::RunScriptMathApiTest` (44 nazwy + Math.Random01 w [0,1] + Math.Noise3D=0 w punkcie siatki, z jawnym `ScriptValue{std::uint32_t{...}}` dowodzącym że walidacja typu UInt32 na pinie faktycznie działa, nie tylko deklaratywna). Build+testy zielone: PEŁNY `cmake --build . --config Debug` (zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe` (oba exit 0).
- [x] LIB-051 Udostępnić `RandomStream` z seedem, snapshotem stanu i funkcjami integer/float/range/shuffle. Decyzja architektoniczna: `kb::math::RandomStream` to `{seed, counter}` — CAŁY stan jako jawna, PRZEZROCZYSTA wartość (nie nieprzezroczysty uchwyt) — dokładnie zgodnie z wymaganiem "snapshotem stanu": snapshot to zwykła kopia struktury, restore to zwykłe przypisanie z powrotem, bez potrzeby dedykowanego API serialize/deserialize. Każda funkcja `Next*` przyjmuje `RandomStream` PRZEZ WARTOŚĆ i zwraca `{wynik, zaawansowany strumień}` — ten sam wzorzec `{value, newState}` co `Damp` (LIB-045), bo LIB-032 zakazuje referencji przez granicę skryptu; wywołujący jawnie "wątkuje" zwrócony strumień do następnego wywołania, nie ma ukrytego kursora. Budowane na `Hash32` z LIB-050 (jedno źródło skramblowania, nie drugie). `NextUInt32` to budulec bazowy ("Integer"); `NextFloat01` [0,1) ("Float"); `NextRange`/`NextIntRange` [min,max) ("Range", degenerate max<=min zwraca min ale WCIĄŻ zaawansowuje strumień, żeby kolejne losowania pozostały zsynchronizowane); `Shuffle<T>` — Fisher-Yates w miejscu. Decyzja zakresu: `Shuffle` jest NATYWNA-TYLKO (szablon C++ nad `std::span<T>`, nigdy nie przekracza granicy skryptu) — `Math.Shuffle` skryptowe wymagałoby widocznego dla skryptu typu kolekcji (`Array<T>`, LIB-058, jeszcze nieistniejący); świadomie NIE fałszowano tego zero-lub-jednoelementowym zamiennikiem. Zarejestrowano w module "Math": `Math.RandomSeed`, `Math.RandomNextUInt32`, `Math.RandomNextFloat01`, `Math.RandomRange`, `Math.RandomRangeInt` — stan strumienia dekomponuje się na piny `streamSeed`/`streamCounter` (oba UInt32, LIB-041), ten sam wzorzec dekompozycji co Vec3/Quat (LIB-048/049) tylko z opisowymi nazwami zamiast prefiksu (bo funkcja Random* zawsze bierze dokładnie jeden strumień). Test: `EngineMathTests.cpp::RunRandomStreamTest` — KLUCZOWA właściwość snapshotu (kopia strumienia wzięta PRZED zaawansowaniem oryginału musi odtworzyć dokładnie tę samą następną wartość, mimo że oryginał później zaawansowano — dowód że stan jest prawdziwą wartością, nie referencją do współdzielonego generatora), determinizm, zakresy float/int, degenerate range wciąż zaawansowuje licznik, Fisher-Yates jest permutacją (suma zachowana) i deterministyczny. Rozszerzony `ScriptRuntimeTests.cpp::RunScriptMathApiTest` (49 nazw + Math.RandomSeed→Math.RandomRangeInt pełny round-trip przez granicę skryptu, dowodzący że wzorzec {value,advancedStream} realnie działa end-to-end, nie tylko natywnie). Build+testy zielone: PEŁNY `cmake --build . --config Debug` (zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe` (oba exit 0).
- [x] LIB-052 Dodać `Easing` jako wartościowy enum/krzywą, bez allocacji callbacków w hot path. Dowód: `kb::math::Easing` — `enum class : std::uint8_t`, DOKŁADNIE ten sam wzorzec co `ScriptValueType`/`LibraryOwnership`/`InputActionValueType` już w kodzie (enum + czysta funkcja `switch`, nie `std::function`/tabela callbacków — brak jakiejkolwiek alokacji, `static_assert(std::is_trivially_copyable_v<Easing>)` jako dowód kompilacyjny, nie tylko deklaracja). Kompletny standardowy katalog "Robert Penner easing" (31 wartości: Linear + In/Out/InOut × {Sine,Quad,Cubic,Quart,Quint,Expo,Circ,Back,Elastic,Bounce}) — plan nie precyzował podzbioru, więc przyjęto konwencjonalny, kompletny zestaw używany w każdym silniku/bibliotece animacji tego typu, żeby nie zostawiać brakujących krzywych do dokładania później. `Evaluate(easing,t)` klampuje WEJŚCIE t do [0,1] (konwencja `Lerp` z LIB-045) ale CELOWO NIE klampuje WYJŚCIA — `InBack`/`OutBack`/`InOutBack` i rodzina `Elastic` z definicji przestrzeliwują poza [0,1] (to jest ich charakterystyczny efekt wizualny "anticipation"/"overshoot"; klampowanie wyniku po cichu złamałoby tę właściwość). Na granicy skryptu `Math.Ease(easing,t)` — `easing` jako `Int` (ordinal), TEN SAM wzorzec co już istniejące pola enum komponentów w `ScriptSceneComponentApi.cpp` (CameraProjection/LightKind rzutowane na Int) — brak dedykowanego `ScriptValueType` dla enumów. Ponieważ `Evaluate()`'s switch nie ma gałęzi `default` (każdy enumerator jawnie obsłużony, więc dodanie nowej wartości Easing bez aktualizacji switcha to błąd kompilacji WSZĘDZIE), rzutowanie niesprawdzonego inta na `Easing` byłoby undefined behavior — `Math.Ease` WALIDUJE ordinal i zwraca prawdziwy błąd domeny (`executed=false`) dla wartości spoza zakresu, ten sam wzorzec co LIB-047's `Math.Asin`/`Math.Acos`. Test: `EngineMathTests.cpp::RunEasingTest` — pętla po WSZYSTKICH 31 krzywych sprawdzająca uniwersalną własność graniczną Penner-easing (f(0)=0, f(1)=1 dla KAŻDEJ), `static_assert` trywialnej kopiowalności, `Linear`=identyczność, `InQuad(0.5)=0.25` (konkretna znana wartość), klampowanie wejścia t, `OutBack` faktycznie przestrzeliwuje >1 (dowód że wyjście NIE jest klampowane). Rozszerzony `ScriptRuntimeTests.cpp::RunScriptMathApiTest` (50 nazw + `Math.Ease` sukces ze znaną wartością + `Math.Ease` z ordinalem poza zakresem→realny błąd domeny). Build+testy zielone: PEŁNY `cmake --build . --config Debug` (zero błędów, zero ostrzeżeń) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe` (oba exit 0).
- [x] LIB-053 Udostępnić `Curve` i `Gradient` z deterministyczną ewaluacją oraz serializacją assetową. Decyzja architektoniczna: `kb::math::Curve`/`Gradient` (`EngineMath.hpp`/`.cpp`) — kolekcje `CurveKeyframe{time,value,easing}`/`GradientStop{time,color}`, REUŻYWAJĄCE `Easing` (LIB-052) jako tryb interpolacji per-segment, zamiast wynajdywać drugi, równoległy enum kształtu krzywej. `Evaluate(curve,t)`/`Evaluate(gradient,t)` — deterministyczne (czysta funkcja danych krzywej + t), t POZA zakresem keyframe'ów jest KLAMPOWANE do pierwszej/ostatniej wartości (nie ekstrapolowane) — ta sama zasada "nie zgaduj poza zdefiniowanymi danymi" co `Lerp`/`InverseLerp` już stosują do własnego t. Serializacja: WŁASNY, samodzielny format binarny (magic number + count + payload) — ŚWIADOMIE NIE reużyto `kb::scene::SceneAssetBinaryIO` mimo że ten moduł jest udokumentowany jako "generyczny" i już reużywany przez `InputAssetIO` — bo `SceneAssetBinaryIO` to PRYWATNY nagłówek `kb::scene` (`sources/engine/src/private/...`), a `kb::math` to fundamentalny moduł zero-zależności (jak każdy inny typ w tym pliku); zależność w górę od `kb::math` do prywatnego szczegółu implementacji `kb::scene` złamałaby granicę publiczne/prywatne. `Serialize`/`Deserialize` zwracają/przyjmują surowe `std::vector<std::uint8_t>` — dowolny kodek assetowy może je wywołać i domkleić bajty do własnego formatu pliku, ten sam sposób reużycia co `SceneAssetBinaryIO`'s helpery, tylko bez zależności strukturalnej. Decyzja zakresu: PEŁNY `IAssetLoader`-zarejestrowany, samodzielnie ładowalny typ assetu (`.curve`/`.gradient` plik) świadomie NIE zaimplementowany — w silniku nie istnieje jeszcze ŻADEN konsument top-level Curve/Gradient assetu (brak systemu cząsteczek, pola krzywej animacji itp.) — budowanie tego łańcucha rejestracji teraz byłoby spekulatywną infrastrukturą bez realnego wywołującego, czego reguły tego repo zakazują. Podobnie: ŻADNA rejestracja `Math.EvaluateCurve`/`Math.EvaluateGradient` w `ScriptMathApi` — `ScriptValue` jest czysto skalarny (LIB-032), więc zmiennodługościowa lista keyframe'ów nie ma jak przekroczyć granicy skryptu bez widocznego dla skryptu typu kolekcji (`Array<T>`, LIB-058, jeszcze nieistniejący) — ten sam wzorzec odłożenia co `Shuffle` w LIB-051, nie fałszowany sztucznym limitem 2-3 keyframe'ów. Test: `EngineMathTests.cpp::RunCurveAndGradientTest` — puste Curve/Gradient nie crashują (zwracają wartość domyślną), dokładne wartości w punktach keyframe'ów (nie blend międzysegmentowy), poprawny wybór segmentu przy 3+ keyframe'ach, klampowanie poza zakresem, PRAWDZIWY round-trip bajtowy Serialize→Deserialize dla obu typów, Deserialize odrzuca puste/ucięte bajty i zły magic number (w tym krzyżowe pomylenie bajtów Curve z Gradient) zamiast czytać poza granicami. Build+testy zielone: PEŁNY `cmake --build . --config Debug` (zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe` (oba exit 0).
- [x] LIB-054 Zdefiniować wartości `NaN`, infinity i zero-length dla wszystkich operacji matematycznych. Charakter zadania: AUDYT + dokumentacja + naprawa realnych niespójności w istniejącym `kb::math` (LIB-041..053), nie nowa funkcjonalność. Przegląd KAŻDEJ funkcji w `EngineMath.hpp`/`.cpp` (delegowany subagentowi researcherowi, potem zweryfikowany ręcznie) ustalił ogólny kontrakt, udokumentowany teraz WPROST komentarzem przy sekcji "scalar math foundation": domyślnie NaN/infinity na WEJŚCIU przepływa BEZ ZMIAN (Clamp/Lerp/InverseLerp/Remap/SmoothStep/Frac/Floor/Ceil/Round/Sqrt/Pow/Exp/Log/arytmetyka Mat4/Easing::Evaluate/Curve-Gradient::Evaluate) — to uczciwy, standardowy kontrakt IEEE-754, ten sam co `Asin`/`Acos` (LIB-047) już ustaliły dla swojej realnej ograniczonej domeny; funkcje robiące COŚ INNEGO (zwracają zdefiniowaną wartość zamiast NaN) dokumentują to wprost przy własnej deklaracji. Zero-length/strukturalnie zdegenerowane wejścia (wektor zerowy, `a==b` w InverseLerp, `divisor==0` w Mod, pusty Curve/Gradient) ZAWSZE zwracają realną, zdefiniowaną wartość — nigdy NaN, nigdy crash — bo to osiągalne, niewyjątkowe stany runtime (niezainicjalizowany Vec3, pusta autorska krzywa), nie błędy wywołującego do odrzucenia jak `Asin`/`Acos` na granicy skryptu. Znaleziono i NAPRAWIONO 3 realne niespójności (nie tylko udokumentowano): (1) `Sign(NaN)` po cichu zwracało `0.0F` zamiast propagować NaN (bo `>`/`<` z NaN są zawsze `false`) — naprawione przez sprawdzenie `value != value` (klasyczna właściwość IEEE-754, działa w `constexpr` bez `std::isnan`); (2) `MoveTowards` z `target=NaN` po cichu zwracało `current+maxDelta` (konkretną, zwodniczo poprawną wartość) zamiast propagować NaN — ta sama naprawa `value != value`; (3) `Mod(x, 0)` dawało NaN (przez `x/0=inf`, `0*inf=NaN`) BEZ ŻADNEGO strażnika — dodano `divisor==0` → zwróć `value` niezmienione (ten sam wzorzec "realny wynik dla zdegenerowanego dzielnika" co już istniejący `InverseLerp`'s `a==b`→0). Dodano też brakujące komentarze dokumentujące próg `Normalize`(Vec3/Quat) przy WŁASNEJ deklaracji (wcześniej udokumentowany tylko pośrednio przez komentarz `Project`), degenerate-input fallback `Angle`/`SignedAngle` (wektor zerowy → 90°, dziedziczone z `Normalize`), i drugą siatkę bezpieczeństwa `Slerp` (końcowe `Normalize` domyka nawet dosłownie zerowy kwaternion wejściowy). Test: `EngineMathTests.cpp::RunNaNInfinityZeroLengthContractTest` — weryfikuje OBIE połówki kontraktu: domyślną propagację NaN (Clamp/Lerp/Floor) ORAZ wszystkie udokumentowane wyjątki (naprawiony `Sign`/`MoveTowards`, strażnik `Mod`, `Normalize`/`Angle`/`Project` dla wejścia zerowego) — z jawnym komentarzem przy każdym "to jest naprawa" tam gdzie dotyczy. Build+testy zielone: PEŁNY `cmake --build . --config Debug` (zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe` (oba exit 0) — zachowanie `Math.Sign`/`Math.MoveTowards`/`Math.Mod` (już zarejestrowane w LIB-046/045/046) automatycznie odziedziczyło naprawy bez zmian w `ScriptMathApi.cpp`.
- [x] LIB-055 Dodać property/fuzz tests dla wektorów, quaternionów, interpolacji i serializacji. Charakter zadania: TESTOWY, nie nowa funkcjonalność — istniejące testy w `EngineMathTests.cpp` (LIB-041..054) były przykładowe (kilka ręcznie dobranych wartości); to zadanie dodaje testy WŁASNOŚCIOWE: setki losowych-ale-DETERMINISTYCZNYCH wejść (z `kb::math::RandomStream`, LIB-051, zasiane stałym literałem — więc porażka jest w 100% odtwarzalna, ta sama sekwencja za każdym razem) sprawdzających ogólne własności matematyczne, nie pojedyncze wartości. Zbadano najpierw `TestSupport.hpp` (delegowane subagentowi) — potwierdzono brak istniejącego frameworka property-testowego, `kb::tests::Require` twardo przerywa proces przy pierwszym niepowodzeniu (brak kontynuacji/agregacji błędów), `NearlyEqual` ma stałą tolerancję 0.0001F (za ciasną dla dużych wartości — użyto własnych tolerancji per-test). 4 nowe funkcje testowe, ~500 iteracji każda: `RunVectorPropertyTest` (przemienność Dot, antyprzemienność Cross, ortogonalność Cross do obu argumentów, nierówność trójkąta dla Length, Normalize zawsze daje length≈1, Reflect zachowuje długość), `RunQuaternionPropertyTest` (znormalizowany kwaternion ma unit length, Rotate(identity,v)=v, rotacja przez unit-kwaternion zachowuje długość wektora, Slerp zakotwicza t=0), `RunInterpolationPropertyTest` (WSZYSTKIE 22 monotoniczne krzywe Easing — celowo WYKLUCZAJĄC Back/Elastic/Bounce, bo te z definicji przestrzeliwują poza [0,1], LIB-052 — pozostają w [0,1] dla losowego t; Lerp zakotwicza t=0/t=1; InverseLerp jest prawdziwą odwrotnością Lerp dla losowego t), `RunSerializationPropertyTest` (100 losowych Curve, każda z 4 keyframe'ami, round-trip Serialize→Deserialize→Evaluate identyczny w 20 losowych punktach t per krzywa — NIE tylko w punktach keyframe'ów, żeby faktycznie przetestować logikę rekonstrukcji interpolacji; DRUGI przebieg Serialize→Deserialize→Serialize daje bajt-w-bajt identyczny wynik, wykrywając nie-deterministyczną "normalizację" danych między przebiegami). ZNALEZISKO PODCZAS PISANIA TESTU (nie błąd w kb::math): pierwsza wersja `Lerp(a,b,1)==b` sprawdzała DOKŁADNĄ równość — failowała dla dużych losowych wartości, bo `a+(b-a)` nie jest gwarantowanie DOKŁADNIE równe `b` w arytmetyce zmiennoprzecinkowej IEEE-754 (nieprzemienność dodawania/odejmowania) — naprawiono zmieniając na tolerancję, NIE zmieniając Lerp (to własność arytmetyki float, nie bug). Build+testy zielone: PEŁNY `cmake --build . --config Debug` (zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe` (oba exit 0, bez zauważalnego spowolnienia — ~2000+ dodatkowych iteracji to tanie operacje float/Vec/Quat).
- [x] LIB-056 Dodać parity tests wyników C++, Lua i Visual Graph z tolerancją float. Charakter zadania: TESTOWY, ostatnie zadanie bloku matematycznego (LIB-041..056 sekcji 5 TERAZ KOMPLETNE). Kluczowa obserwacja przed implementacją: wywołanie tej samej zarejestrowanej funkcji `ScriptFunctionRegistry` przez Native/Lua/Visual Graph to test parytetu ŚCIEŻEK MARSHALLINGU (ScriptExecutionContext::CallFunction, Lua `CallFunction` global, VisualGraph `CallNative` binding), NIE parytetu samej matematyki `Math.Clamp` — ta jest już pokryta natywnie w `EngineMathTests.cpp`; wszystkie 3 ścieżki i tak wołają dokładnie ten sam `kb::math::Clamp` pod spodem przez `ScriptMathApi.cpp`'s callback. Znaleziono i zreużyto PRECEDENS: `RunScriptFunctionRegistryCrossBackendTest` (LIB-011, `Inventory.AddItem`) już ustanowił dokładnie ten trójskładnikowy harness (3 encje z `BehaviourComponent` Native/Lua/VisualGraph, jeden `ScriptRuntime::ExecuteLifecycle` Tick, wynik odczytany przez `SharedState`) — nowy test `RunMathFunctionCrossBackendParityTest` powtarza go 1:1 z `Math.Clamp`'s trzema wejściami Float (value/min/max) zamiast pojedynczego Int (`itemId`) `Inventory.AddItem`. Native: `context.CallFunction("Math.Clamp", ...)` wewnątrz `NativeScriptBackend::RegisterLifecycle`. Lua: rzeczywisty kod Lua `CallFunction("Math.Clamp", {value=15.0,min=0.0,max=10.0})` wykonany przez `PucLuaScriptRuntime::LoadScript`. Visual Graph: prawdziwy skompilowany graf (`VisualGraphCompiler::Compile`) z węzłem `CallNative` symbol=`"Function.Math.Clamp"` (auto-zarejestrowany binding, bo `ScriptRuntimeHost::RegisterFunction` już generuje bindingi VisualGraph dla KAŻDEJ funkcji w rejestrze — zero dodatkowego kodu bindującego potrzebnego) zasilanym stałymi przez jeden `GetProperty` binding z 3 wyjściami (zamiast 3 osobnych węzłów). Wszystkie 3 wyniki porównane MIĘDZY SOBĄ i z oczekiwaną wartością `Clamp(15,0,10)=10` przez `kb::tests::NearlyEqual` (tolerancja 0.0001F) — prawdziwa "tolerancja float" z nazwy zadania, nie dokładna równość. Test: `ScriptRuntimeTests.cpp::RunMathFunctionCrossBackendParityTest` — przeszedł za pierwszym razem, potwierdzając że cała infrastruktura marshallingu `Math.*` (LIB-045..052) działa identycznie przez wszystkie 3 ścieżki. Build+testy zielone: PEŁNY `cmake --build . --config Debug` (zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe` (oba exit 0). BLOK MATEMATYCZNY SEKCJI 5 (LIB-041..056, 16 zadań) TERAZ W PEŁNI KOMPLETNY — dalej sekcja 5 kontynuuje się podsekcją "Kolekcje i tekst" (LIB-057..064).

### Kolekcje i tekst

- [x] LIB-057 Udostępnić niemutowalny `ArrayView<T>` dla danych zwracanych przez runtime. Decyzja architektoniczna (zbadana przed implementacją): `std::span<const T>` jest JUŻ de facto konwencją "niemutowalnego widoku" w tym silniku — 192 pliki go używają, w tym publiczne API zwracające dane runtime (`kb::scene::SceneHierarchyAccess`, `kb::assets::AssetRegistry::All()`, `kb::ecs::QueryFilter`, `ScenePrefabInstance::Objects()`); `kb::library` samo już go używa (`EngineLibraryModuleValidation`). Zero istniejącego dedykowanego typu `ArrayView` w kodzie. LIB-057 NIE wynajduje nowego mechanizmu bezpieczeństwa granic — formalizuje istniejącą konwencję jako publiczny kontrakt `kb::library`, TĄ SAMĄ metodą co `AssetRef<T>` (LIB-009, alias do `kb::assets::AssetHandle<T>`, nie duplikat). Dowód: `kb::library::ArrayView<T> = std::span<const T>` (`EngineLibraryArrayView.hpp`) — "niemutowalny" wymuszony PRZEZ KONSTRUKCJĘ aliasu (zawsze `const T`, niezależnie od T), nie przez konwencję nazewniczą którą wywołujący mógłby zignorować. Test: `EngineLibraryTests.cpp::RunArrayViewTest` — `static_assert` że alias to dokładnie `std::span<const T>` (nie duplikat), `static_assert` że `operator[]` zwraca `const T&` NAWET gdy skonstruowany z mutowalnego `std::vector<T>` (mutowalność źródła nie przecieka przez widok) — dowód kompilacyjny, nie tylko dokumentacja; konstrukcja z `std::vector`, iteracja range-for, indeksowanie; oraz PRAWDZIWY wynik zapytania runtime (`kb::scene::SceneHierarchyAccess::RootEntities()`, zwracający `std::vector<SceneEntity>`) użyty JAKO `ArrayView` bez żadnego kroku adaptującego/kopiującego — dokładnie "dane zwracane przez runtime" z nazwy zadania. Build+testy zielone: PEŁNY `cmake --build . --config Debug` (zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe` (oba exit 0). POCZĄTEK podsekcji "Kolekcje i tekst" (LIB-057..064).
- [x] LIB-058 Udostępnić kontrolowany `Array<T>`, `Map<K,V>`, `Set<T>`, `Queue<T>` i `Stack<T>` dla danych skryptowych. Decyzja architektoniczna: `sources/engine/include/engine/library/EngineLibraryCollections.hpp` (header-only, 5 szablonów C++, generyczne nad DOWOLNYM T z `operator==`, w tym `kb::script::ScriptValue`, dowodząc "dla danych skryptowych"). "Kontrolowany" = KAŻDA mutacja rosnąca kolekcję zwraca `bool` i ODMAWIA przekroczenia twardej pojemności zamiast rosnąć bez ograniczeń lub rzucać wyjątek; konstruktor każdego typu KLAMPUJE żądaną pojemność do `kDefaultLibraryInputLimits.maxCollectionSize` (4096, LIB-037's zarezerwowana wartość polityki — TERAZ realnie egzekwowana, nie tylko zarezerwowana) — zasady nie da się obejść przez podanie większej liczby per wywołanie. `Set<T>`/`Map<K,V>` są backed by `std::vector` z LINIOWYM (O(n)) wyszukiwaniem przez `operator==`, NIE `std::unordered_*` — świadoma decyzja: `ScriptValue` ma `operator==` ale NIE ma `std::hash` (dodanie specjalizacji `std::hash<ScriptValue>` byłoby scope creep tego zadania i osobną decyzją o tym co właściwie hashować przy tagged-union), więc wybrano reprezentację generyczną nad DOWOLNYM T z `operator==`; kolejność iteracji jest WSTAWIENIOWA jako efekt uboczny reprezentacji, nie gwarancja — koszt alokacji i formalna gwarancja deterministycznej iteracji to jawnie LIB-059/LIB-060, nie ten task. Świadomie POZA zakresem (udokumentowane, nie ciche): przekraczanie granicy skryptu Lua/Visual Graph przez UCHWYT do kolekcji (np. `Array.Create`/`Array.GetAtInt` wywoływalne wprost ze skryptu) — wymagałoby nowych `ScriptValueType` enumeratorów, per-`ScriptRuntime` rejestru uchwytów z walidacją generacji (wzorowanego na `EntityHandle::IsAlive`/`Validate`/`CheckError`) i aktualizacji ~19 exhaustive switchy nad `ScriptValueType` (jak LIB-041) — to osobna, duża decyzja architektoniczna, świadomie odłożona (nie fałszowana małym podzbiorem funkcji); odłożone razem z nią pozostają `Math.Shuffle` (LIB-051) i `Math.EvaluateCurve`/`Math.EvaluateGradient` (LIB-053) jako wywoływalne ze skryptu — Array<T> jako TYP teraz istnieje i jest przetestowany z `ScriptValue`, ale most przez granicę skryptu wciąż nie istnieje. Test: `EngineLibraryTests.cpp::RunCollectionsScalarTest` (kontrakt pojemności/mutacji wszystkich 5 typów nad `int`/`std::string`, w tym odmowa wzrostu ponad pojemność, klampowanie żądania powyżej `maxCollectionSize`) oraz `RunCollectionsScriptValueTest` (te same 5 typów nad `ScriptValue` — w tym `Set<ScriptValue>` odróżniające `Hash` od `Entity` mimo tego samego surowego id, `Map<ScriptValue,ScriptValue>` z kluczem `Name`, `Array<ScriptValue>` przechowujące HETEROGENICZNIE otypowane elementy w jednej tablicy). Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wszystkie testy, zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe` (oba exit 0).
- [x] LIB-059 Ustalić koszt alokacji kolekcji i warianty `NonAlloc`/caller-provided buffer dla hot path. Koszt alokacji USTALONY dla `Array`/`Set`/`Map`/`Stack` (LIB-058): każdy konstruktor teraz woła `reserve(capacity_)` na wektorze bazowym — DOKŁADNIE JEDNA alokacja przy konstrukcji, zero realokacji przy dowolnym wypełnianiu aż do zadeklarowanej pojemności; nowy publiczny `AllocatedCapacity()` (rzeczywista `vector::capacity()`) czyni to TESTOWALNYM twierdzeniem, nie tylko komentarzem — `RunCollectionsAllocationCostTest` przechwytuje `AllocatedCapacity()` zaraz po konstrukcji i wymaga, by pozostała identyczna po każdym kolejnym wstawieniu aż do pojemności. `Queue<T>` UCZCIWIE udokumentowany jako WYJĄTEK: backed by `std::deque` (O(1) push/pop na obu końcach), który NIE MA `reserve()` — koszt to sekwencja alokacji bloków o stałym rozmiarze w miarę wzrostu, nie jedna alokacja z góry; to świadomy kompromis (struktura chunked deque JEST tym, co daje O(1) na obu końcach), nie przeoczenie. Warianty `NonAlloc`: 5 nowych szablonów w TYM SAMYM `EngineLibraryCollections.hpp` — `ArrayNonAlloc<T>`, `SetNonAlloc<T>`, `MapNonAlloc<K,V>`, `QueueNonAlloc<T>`, `StackNonAlloc<T>` — każdy operuje WYŁĄCZNIE nad `std::span<T>` dostarczonym przez wywołującego (tablica na stosie, bufor thread_local, wycinek frame arena), NIGDY nie alokuje własnej pamięci; pojemność = rozmiar dostarczonego bufora (brak osobnego parametru pojemności do klampowania — bufor wywołującego JEST granicą). `QueueNonAlloc` to PRAWDZIWY ring buffer nad span (head/count, modulo indeksowanie) — O(1) enqueue/dequeue, LEPSZY dla hot path niż właściwy `Queue<T>` powyżej, który alokuje. Test: `RunCollectionsAllocationCostTest` (dowód jednej alokacji dla 4 z 5 typów, jak wyżej) oraz `RunCollectionsNonAllocTest` (kontrakt pojemności/mutacji wszystkich 5 wariantów NonAlloc nad `std::array` jako storage wywołującego, w tym dowód że zapisują BEZPOŚREDNIO do bufora wywołującego — `arrayStorage[0]`/`stackStorage[1]` odczytane po mutacji — oraz explicit test wraparound ring buffera `QueueNonAlloc` dowodzący prawdziwej cykliczności, nie tylko działania przy pierwszym przejściu przez bufor). Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wszystkie testy, zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe` (oba exit 0).
- [x] LIB-060 Ustalić deterministyczną iterację map/setów albo zakazać polegania na jej kolejności. Decyzja: SFORMALIZOWANO gwarancję (nie zakazano polegania na kolejności) — `Set<T>`/`Map<K,V>` (LIB-058) i ich warianty `SetNonAlloc`/`MapNonAlloc` (LIB-059) już były wstawieniowo deterministyczne jako PRZYPADKOWA własność reprezentacji vector-backed; LIB-060 podniosło to do JAWNEGO, udokumentowanego i przetestowanego kontraktu, bo utrzymanie tej gwarancji nie kosztuje nic dodatkowego (żadna operacja nigdy nie używała swap-with-back, tylko erase-w-miejscu). Dokładna reguła udokumentowana w `EngineLibraryCollections.hpp`: aktualni członkowie/klucze pojawiają się w kolejności NAJNOWSZEGO (re-)wstawienia; `Insert`/`Set` zawsze dokleja nowy element na końcu i NIGDY nie przestawia istniejących (aktualizacja wartości pod istniejącym kluczem w `Map::Set` zostaje na swojej pozycji); `Remove` zawsze usuwa w miejscu (przesuwając późniejsze elementy w dół), nigdy nie przestawia pozostałych; usunięcie i ponowne wstawienie TEJ SAMEJ wartości/klucza NIE przywraca starej pozycji — ląduje na końcu jak każde nowe wstawienie (bo z punktu widzenia kolekcji jest w tym momencie po prostu nieobecny, a potem nowo dodany). Test: `EngineLibraryTests.cpp::RunCollectionsDeterministicIterationTest` — dokładna sekwencja insert×3→remove środkowego→re-insert dla `Set<int>` i `Map<int,int>` (w tym dowód, że `Map::Set` na ISTNIEJĄCYM kluczu NIE przestawia go), powtórzona NIEZALEŻNIE dla `SetNonAlloc`/`MapNonAlloc` (osobne implementacje — błąd w jednej nie zostałby złapany testem tylko drugiej). Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wszystkie testy, zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe` (oba exit 0).
- [x] LIB-061 Udostępnić `Option<T>`/`Result<T,E>` w językach, które je wspierają, i idiomatyczne adaptery Lua/graph. Decyzja architektoniczna (zbadana przed implementacją): Native C++ — jedyny język w silniku z realnym wsparciem sum-type — JUŻ MA oba: `std::optional<T>` (używany wszędzie: `ScriptSharedState::Get`, `EntityHandle::CheckError`) TO `Option<T>`, `kb::library::Result<T>` (LIB-036, T-lub-ScriptError) TO `Result<T,E>`; udokumentowano to explicite w `EngineLibraryResult.hpp` zamiast fabrykować nowy typ. Ani Lua ani Visual Graph nie mogą wyrazić generycznego szablonowanego sum-type przez granicę skryptu (`ScriptValue` to stały, czysto skalarny tagged union, LIB-032/LIB-041) — więc "udostępnić" dla tych dwóch oznacza IDIOMATYCZNY ADAPTER PER JĘZYK, nie ten sam typ generyczny. Lua: `value, err = CallFunction(...)` (nil+string błędu przy porażce, realna wartość/tabela przy sukcesie) JUŻ ISTNIAŁ w kodzie (`PucLuaFunctionApi.cpp::LuaCallFunction`) ale bez żadnego testu wykonującego PRAWDZIWY kod Lua przez tę ścieżkę — dodano `RunLuaCallFunctionResultAdapterTest`, pierwszy test end-to-end (rzeczywisty skrypt Lua wywołujący zarówno failing jak i succeeding funkcję, odczyt przez SharedState, weryfikacja nil-przy-porażce/wartość-przy-sukcesie). Visual Graph: PRAWDZIWA NOWA FUNKCJONALNOŚĆ — węzeł `CallNative` dostał drugi exec output `"failed"` obok istniejącego `"then"` (sukces), analogicznie do pary `"true"`/`"false"` węzła `Branch` (ten sam, już istniejący prymityw rozgałęzienia wykonania — `VisualGraphIrInstruction::trueNodeId/falseNodeId`, `VisualGraphCompiler.cpp`'s `FirstOutgoingNode` per nazwany pin — nie wynaleziono nowego mechanizmu). Zmiany: `VisualGraphNodeDefinitionRegistry.cpp` (3. pin), `VisualGraphCompiler.cpp` (helpery `HasExecutionBranches`/`SuccessPinFor`/`FailurePinFor`, rozszerzają istniejące miejsca liczące trueNodeId/falseNodeId o CallNative obok Branch), `VisualGraphRuntimeExecutor.cpp::ExecuteNode` (wydzielona ścieżka CallNative: werdykt sukces/porażka = czy callback dodał NOWY błąd do `context.RuntimeErrors()` — TYLKO delta od `errorCountBefore`, nigdy cała lista, bo inaczej węzeł osiągnięty PO obsłużonej porażce fałszywie odziedziczyłby cudzy błąd; werdykt zapisany przez `context.Store(nodeId,"failed",...)` żeby przetrwać ponowne odwiedziny węzła). DECYZJA: porażka ZAWSZE trafia do `result` jako błąd (niezależnie czy "failed" jest podpięte) — bo `ScriptDiagnostic` (warstwa `ScriptRuntimeExecutionResult`) NIE MA pola severity (`Succeeded()` na KAŻDYM poziomie to dosłownie `diagnostics.empty()`), więc rozróżnienie Error/Warning na poziomie `VisualGraphDiagnostic` i tak zniknęłoby przy spłaszczeniu do `ScriptDiagnostic` (`VisualGraphScriptBackend::ToScriptResult`) — dodano i POTEM ŚWIADOMIE USUNIĘTO nieużywaną `VisualGraphDiagnostics::Warning` po odkryciu tego faktu, żeby nie zostawić martwego API. Realna wartość funkcji: podpięty "failed" NIE zmienia że `Succeeded()==false` po prawdziwej porażce (uczciwe, zgodne z resztą silnika), ale SPRAWIA że handler faktycznie się wykonuje (logowanie, fallback, telemetria) zamiast całego Ticka zatrzymującego się w miejscu porażki; niepodpięty "failed" zachowuje dokładnie dawne zachowanie fail-loud (zweryfikowane: ISTNIEJĄCY test `RunScriptFunctionRegistryCrossBackendTest`'s failing-function case przeszedł BEZ ŻADNEJ zmiany). Realny bug znaleziony i naprawiony PODCZAS implementacji (nie po fakcie): pierwsza wersja dispatchu branch dla CallNative robiła `return ExecuteNode(...)` bezpośrednio zamiast scalać z lokalnym `result` — gubiąc właśnie zarejestrowany błąd obsłużonej porażki (bezpieczne dla Branch, bo tam `result` jest zawsze pusty w tym miejscu; NIE dla CallNative) — naprawione przez `AppendErrors(result, ExecuteNode(...)); return result;`, ten sam wzorzec co generyczna ścieżka końcowa. Test: `VisualGraphTests.cpp`'s `callPins.size()` zaktualizowany 2→3 + assercja obecności pinu "failed"; NOWY `ScriptRuntimeTests.cpp::RunVisualGraphCallNativeFailureBranchTest` — prawdziwy graf skompilowany i wykonany przez `ScriptRuntimeHost`, dowodzi że: (1) ścieżka sukcesu NIE wykonuje się po porażce, (2) podpięty handler porażki WYKONUJE SIĘ (realny efekt uboczny, nie tylko brak crasha), (3) `Succeeded()==false` mimo obsłużenia, (4) komunikat błędu trafia do diagnostyki. Świadomie POZA zakresem: natywny codegen (`VisualGraphNativeCodeGenerator.cpp`) — zbadano i potwierdzono że `VisualGraphNativeExecutionContext`/`VisualGraphNativeFunctionResult` to CAŁKOWICIE generowany kontrakt interfejsu (deklaracje bez JAKIEJKOLWIEK implementacji gdziekolwiek w repo) używany WYŁĄCZNIE do asercji tekstowych w testach (`generated.source.find(...)`), nigdy faktycznie kompilowany+uruchamiany — dodawanie rozgałęzienia sukces/porażka do spekulatywnego, nietestowanego na żywo kontraktu byłoby dokładnie tym czego reguły repo zakazują (infrastruktura bez realnego konsumenta), odłożone do czasu realnego hosta zewnętrznego konsumującego wygenerowany kod. Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wszystkie testy, zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe`+`kb_editor_tests.exe` (wszystkie exit 0).
- [x] LIB-062 Udostępnić formatowanie tekstu bez niekontrolowanej alokacji w pętli klatki. Dowód: `sources/engine/include/engine/library/EngineLibraryTextFormat.hpp` (header-only) — `kb::library::TextFormatBuffer` opakowuje `std::span<char>` dostarczony przez wywołującego (tablica na stosie, bufor thread_local, wycinek frame arena) i SAM NIGDY nie alokuje pamięci — ten sam kontrakt "kontrolowany"/NonAlloc co `EngineLibraryCollections.hpp` (LIB-059), zastosowany do tekstu zamiast kolekcji. `Append*` zwraca `bool` (true=zmieściło się, false=NIE zmieściłoby się i NIC nie zostało zapisane — nigdy częściowy/ucięty zapis, zweryfikowane testem czytającym surowe bajty bufora PO odrzuconym Append). Konwersje liczbowe (`AppendInt`/`AppendUInt`/`AppendFloat`) idą przez `std::to_chars` (C++17) — standard GWARANTUJE brak alokacji, w przeciwieństwie do `std::to_string`/snprintf-style (mogą alokować wewnętrznie) — to właśnie "niekontrolowana alokacja" z nazwy zadania. Decyzja zakresu: API to sekwencyjne `Append*` (tekst/int/uint/float/bool/char) budujące string przyrostowo, NIE pełny mini-język formatowania w stylu printf/`std::format` — świadomie, żeby uniknąć wynajdywania drugiego parsera format-stringów; to wystarcza do typowego przypadku użycia z nazwy zadania (budowanie stringu HUD/debug per klatkę). Test: `EngineLibraryTests.cpp::RunTextFormatBufferTest` — dokładne formatowanie (ujemne int, uint powyżej zakresu int32, bool jako "true"/"false", float z zadaną precyzją), Clear resetuje Length, oraz KLUCZOWY dowód braku częściowego zapisu: `Append` który by się nie zmieścił zwraca false, `Length()`/`View()` niezmienione, a SUROWE bajty bufora POZA `Length()` sprawdzone wprost (`smallStorage[2]=='X'`) żeby udowodnić że nic nie zostało zapisane poza logiczną długością. Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wszystkie testy, zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe` (oba exit 0).
- [x] LIB-063 Udostępnić parsing liczb, GUID, kolorów i dat z jednoznaczną lokalizacją/invariant culture. Dowód: `sources/engine/include/engine/library/EngineLibraryParsing.hpp`+`.cpp` — `TryParseInt64`/`TryParseUInt64`/`TryParseDouble` (przez `std::from_chars`, C++17 — standard GWARANTUJE niezależność od locale, w przeciwieństwie do `std::atof`/`std::strtod`/`std::stringstream`, których zachowanie zależy od globalnego locale procesu, np. separator dziesiętny ',' vs '.'), `TryParseGuid` (kanoniczna forma 8-4-4-4-12 hex z myślnikami, 36 znaków, case-insensitive — ZAMYKA odłożoną z LIB-041 walidację formatu Guid, którą komentarz konstruktora `ScriptValue.hpp` explicite zostawił temu zadaniu), `TryParseColor` (hex "#RRGGBB"/"#RRGGBBAA" → `kb::math::Color`, kanał/255.0, alfa domyślnie nieprzezroczysta zgodnie z domyślną wartością `Color`), `TryParseDate` (ISO 8601 "YYYY-MM-DD" → `std::chrono::year_month_day` — REUŻYCIE typu standardowego zamiast wynajdywania własnej struktury Date, bo `.ok()` już poprawnie implementuje rzeczywistą walidację kalendarzową, w tym lata przestępne — nie reimplementowano tej logiki). Wszystkie 6 funkcji: `bool`-returning, NIGDY nie rzucają, NIGDY częściowo nie zapisują wyniku przy porażce (parametr wyjściowy dotykany TYLKO przy sukcesie), ten sam kontrakt `TryParse*` co istniejący w silniku (`TryParseVisualGraphValueType`, `ScriptAssetLoader::TryParseScriptValueType`). Test: `EngineLibraryTests.cpp::RunParsingTest` — poprawne wartości dla każdej z 6 funkcji, ODRZUCENIE pustego stringa/końcowego śmiecia/wiodącej białej spacji dla liczb (dowód że `from_chars` nie "zgaduje", wymaga PEŁNEGO zużycia stringa), odrzucenie GUID o złej długości/złych pozycjach myślników/nie-hex znaku, odrzucenie Color bez '#'/złej długości/nie-hex znaku, oraz KLUCZOWY dowód realnej walidacji kalendarzowej Date (nie tylko formatu): "2023-02-30" odrzucone (luty nie ma 30 dni), "2024-02-29" zaakceptowane (rok przestępny), "2023-02-29" odrzucone (NIE rok przestępny) — udowadnia że `.ok()` faktycznie liczy, nie tylko sprawdza zakres cyfr. Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wszystkie testy, zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe` (oba exit 0).
- [x] LIB-064 Dodać UTF-8 jako jedyny format publicznych stringów oraz walidację granicy platformy. Dowód: `sources/engine/include/engine/library/EngineLibraryTextEncoding.hpp`+`.cpp` — `kb::library::IsValidUtf8(std::string_view)`, PRAWDZIWY walidator bajtowy zgodny z tabelą walidacji Unicode (nie "czy każdy bajt wygląda wiarygodnie"): odrzuca overlong encoding (np. 0xC0 0x80 dla NUL), zakodowane połówki surogatów UTF-16 (U+D800..U+DFFF — poprawny UTF-8 nigdy ich nie reprezentuje), kodpunkty powyżej U+10FFFF, ucięte sekwencje wielobajtowe i błędne/samotne bajty kontynuacji. Decyzja "granicy platformy": WPIĘTE w `kb::script::ScriptFunctionRegistry::ValidateInputs` — TEN SAM punkt kontrolny co limit długości stringa z LIB-037 (Native+Lua+Visual Graph, jedyne miejsce przez które KAŻDA publiczna wartość String faktycznie przechodzi) — to właśnie "granica platformy" w tym silniku: miejsce gdzie dane skryptowe/zewnętrzne wchodzą do silnika, analogicznie do LIB-032's granicy skryptu dla wskaźników. Świadomie NIE dodano ogólnej konwersji UTF-8↔UTF-16 dla Windows (istnieje już PRYWATNY, edytorowy `ScriptEditorTextEncoding::Widen/Narrow` dla widgetu edytora skryptów — nie generalizowano go do `kb::library`, bo żaden NOWY konkretny wywołujący tego dziś nie potrzebuje; budowanie tego teraz byłoby spekulatywną infrastrukturą, ten sam wzorzec odłożenia co wcześniejsze decyzje w tej sesji). Test: `EngineLibraryTests.cpp::RunUtf8ValidationTest` — akceptacja WSZYSTKICH poprawnych długości sekwencji (1-4 bajty) i dokładnie maksymalnego kodpunktu U+10FFFF, odrzucenie każdego z 5 udokumentowanych wzorców błędu (overlong/ucięta/samotna kontynuacja/surogat/poza zakresem) osobnym przypadkiem testowym z konkretnymi bajtami, ORAZ prawdziwy test end-to-end przez `ScriptFunctionRegistry::Call` dowodzący że zniekształcony UTF-8 argument String jest odrzucony przez PRAWDZIWE wywołanie zarejestrowanej funkcji (nie tylko przez sam walidator w izolacji), a poprawny UTF-8 nadal przechodzi. Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wszystkie testy, zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe`+`kb_editor_tests.exe` (wszystkie exit 0) — potwierdza że nowa walidacja nie złamała ŻADNEGO z setek istniejących testów Native/Lua/VisualGraph gdziekolwiek w silniku. KOMPLETNA podsekcja "Kolekcje i tekst" (LIB-057..064, 8 zadań) TERAZ W PEŁNI ZAKOŃCZONA.

## 6. Świat, entity, komponenty i transformacje

### World i Scene

- [x] LIB-065 Udostępnić `World.Current`, `World.IsPlaying`, `World.FrameIndex` i `World.FixedStepIndex`. POCZĄTEK sekcji 6 ("Świat, entity, komponenty i transformacje") — kontynuacja ISTNIEJĄCEGO modułu `ScriptWorldApi.cpp`/"World" (już zawierał World.FindByName/Exists/Name/Spawn/Destroy/SetTag/HasTag/FindByTag/SetParent/InstantiatePrefab z wcześniejszych zadań fundamentowych), nie nowy moduł. Zbadano przed implementacją: `World.Current` — trywialne, `Scene::Id()` już istnieje (runtime instance id, celowo NIE to samo co `kb::library::SceneRef`/`AssetRef<SceneDocument>` — SceneRef nazywa ASSET na dysku, `Scene::Id()` to ŻYWA, uruchomiona instancja świata w pamięci; zwrócone jako `ScriptValueType::Hash`, bo to opakowany identyfikator, nie wartość arytmetyczna). `World.IsPlaying`/`FrameIndex`/`FixedStepIndex` — ŻADEN nie miał istniejącego stanu silnika do wystawienia (zbadano: "IsPlaying" istniało TYLKO jako `EditorPlayModeState`, edytorowy, nieosiągalny z `kb::script`; `lastFixedStepCount` istniał ale resetuje się co klatkę, nie jest monotoniczny) — dodano 3 NOWE pola do `SceneState` (`frameIndex`, `fixedStepIndex` — monotoniczne, NIGDY nie resetowane, w przeciwieństwie do `lastFixedStepCount`; `isPlaying`, domyślnie `true`) + `SceneRuntimeService::FrameIndex/FixedStepIndex/IsPlaying/SetPlaying` (wzorem istniejącego `LastFixedStepCount`) + publiczne akcesory na fasadach `SceneRuntime`/`SceneRuntimeQueries` (`sources/engine/include/engine/scene/SceneRuntime.hpp`). `frameIndex` inkrementowany raz na `Update()` (bezwarunkowo, także dla scen PrefabPrivate); `fixedStepIndex` inkrementowany raz na FAKTYCZNY fixed-step (`++state.fixedStepIndex` obok istniejącego `++state.lastFixedStepCount`). Decyzja zakresu: `World.IsPlaying` to NOWA flaga na poziomie silnika (`Scene::Runtime().SetPlaying(bool)`), świadomie NIEPODŁĄCZONA do edytorowego przycisku Play/Stop (`EditorPlayModeState`) — to osobna integracja UI/UX edytora, nie rdzeń `kb::library`; flaga silnikowa istnieje i jest odczytywalna ze skryptu, weryfikowana testem, ale realne wpięcie edytora pozostaje odłożone (udokumentowane, nie ciche). Ryzyko nazw: LIB-093 (jeszcze nieodhaczone) definiuje PÓŹNIEJ `Time.FrameIndex`/`Time.FixedStepIndex` — prawdopodobnie inny (globalny/silnikowy) zakres niż `World.FrameIndex`/`FixedStepIndex` (per-scena) — udokumentowane w `_temp.md` jako POWRÓT do sprawdzenia przy LIB-093. Test: `SceneSystemTransformSyncTests.cpp::RunSceneRuntimeFrameAndPlayStateTest` — REUŻYWA dokładnie tę samą fixture co istniejący `RunSceneRuntimeFixedStepTest` (3×Update z konkretnymi deltaSeconds), weryfikuje dokładne skumulowane wartości frameIndex (1,2,3) i fixedStepIndex (0,2,6 — dowód że NIE resetuje się między klatkami, w przeciwieństwie do lastFixedStepCount), domyślne IsPlaying==true i SetPlaying toggle. `ScriptRuntimeTests.cpp::RunScriptWorldTimePhysicsApiTest` rozszerzony o prawdziwe wywołania przez `ScriptFunctionRegistry::Call` (nie tylko bezpośrednio przez Scene API) dla wszystkich 4 nowych funkcji, w tym World.Current zwracające `scene.Id()`. Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wszystkie testy, zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe`+`kb_editor_tests.exe` (wszystkie exit 0).
- [x] LIB-066 Udostępnić `World.Spawn(prefab, pose, parent?)` zwracające uchwyt dopiero po zdefiniowanym flushu. Zbadano przed implementacją (kluczowe dla decyzji zakresu): tworzenie encji w tym silniku jest ZAWSZE NATYCHMIASTOWE (nie deferred/queued) — `SceneEntityCreationService::CreateEntity`, `kb::ecs::World::CreateEntity()`, a nawet wewnętrzny `CommandBuffer` w `ScenePrefabBulkInstantiationService::Instantiate` jest odtwarzany (`Playback`) SYNCHRONICZNIE przed zwróceniem — więc "flush" z nazwy zadania NIE dotyczy istnienia encji (to już zawsze gotowe), tylko TRANSFORMU ŚWIATOWEGO, który jest leniwie/dirty-flag synchronizowany przez `SceneTransformHierarchySystem` i mógłby być nieaktualny (stale) dla świeżo utworzonej/przepiętej encji dopóki coś nie wywoła `SynchronizeTransforms()`. Decyzja: rozszerzono ISTNIEJĄCĄ funkcję `World.Spawn` (nie stworzono nowej/nadpisanej — `ScriptFunctionRegistry` nie wspiera przeciążeń po nazwie) o: nowy opcjonalny pin `prefab` (String — pusty = zachowanie sprzed LIB-066 bez zmian, tworzy pustą encję, ŚCIEŻKA ZACHOWANA BAJT-W-BAJT dla wstecznej kompatybilności; niepusty = ładuje i instancjuje prefab, ta sama logika co `World.InstantiatePrefab`), 4 nowe piny rotacji `rotX/Y/Z/W` (druga połowa `kb::math::Pose{position, rotation}` — pozycja już istniała jako x/y/z, ten sam wzorzec dekompozycji Vec3/Quat na piny co LIB-048/049), `parent` już istniał. Na końcu funkcji (OBIE ścieżki, blank i prefab) dodano `context.scene->Runtime().SynchronizeTransforms()` PRZED zwróceniem uchwytu — to jest KONKRETNA implementacja "zdefiniowanego flushu": gwarantuje że `worldPosition`/`worldRotation` zwróconej encji (nie tylko `local*`) są poprawne NATYCHMIAST, bez potrzeby wywoływania przez skrypt dodatkowego `Update()`. Test: `ScriptRuntimeTests.cpp::RunScriptWorldTimePhysicsApiTest` rozszerzony — prawdziwe wywołanie `World.Spawn(prefab="/Game/Prefabs/RuntimePrefab.kbprefab", parent=enemy, x=5, rotW=1, ...)` przez `ScriptFunctionRegistry::Call`, weryfikujące: poprawną nazwę/tagi odziedziczone z prefaba (dowód że dane prefaba faktycznie się zachowują), poprawnego rodzica przez `Hierarchy().Parent()`, poprawną pozycję/rotację LOCAL, oraz KLUCZOWY dowód flushu — `worldPosition.x == 7.0` (rodzic na x=2 + lokalny offset x=5) odczytane BEZPOŚREDNIO PO `World.Spawn`, bez żadnego pośredniego `Update()`/`SynchronizeTransforms()` wywołanego przez test. Cała ISTNIEJĄCA ścieżka blank-entity (bez `prefab`) zweryfikowana jako niezmieniona przez pełny przebieg wszystkich wcześniejszych testów `World.Spawn` (Native direct-call i Lua) bez żadnej modyfikacji ich kodu. Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wszystkie testy, zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe`+`kb_editor_tests.exe` (wszystkie exit 0).
- [x] LIB-067 Udostępnić `World.Destroy(entity)` z idempotencją i flagą deferred/immediate zgodną z ECS. Zbadano przed implementacją: idempotencja JUŻ ISTNIAŁA (`Destroy()` sprawdza `IsAlive` przed zniszczeniem, powtórne wywołanie zwraca `destroyed=false` zamiast błędu lub crasha) — brakowało tylko testu to dowodzącego. "Flaga deferred/immediate zgodna z ECS": zbadano `kb::ecs::CommandBuffer` (realny, istniejący mechanizm deferred) — NIE podłączony do ŻADNEJ fazy cyklu życia (`CommandApplicationPointFor(phase)` zwraca `Immediate` dla każdej fazy, LIB-006, wciąż prawdziwe) i BEZ właściciela/punktu odtwarzania między klatkami; co więcej WŁASNA ścieżka destroy CommandBuffera NIE jest bezpieczna wobec encji już zniszczonej inną ścieżką przed odtworzeniem (`Playback()` rzuca `std::out_of_range` na "stale entity handle"). Podłączenie prawdziwego mechanizmu deferred z realnym punktem odtwarzania to JAWNIE zakres LIB-083 (wciąż otwarte, niski priorytet), NIE tego zadania. Decyzja: flaga `deferred` (Bool, opcjonalna) DODANA do sygnatury (realny, udokumentowany input, spełnia dosłowne brzmienie zadania) — `deferred=true` UCZCIWIE ODRZUCONE z jasnym komunikatem błędu (cytującym LIB-006/LIB-083) zamiast cicho zachowywać się jak immediate (co byłoby fałszywym sukcesem) albo budować na poczekaniu połowicznie działający bufor z realnym ryzykiem crasha przy współbieżnym zniszczeniu tej samej encji dwoma ścieżkami. `deferred=false`/pominięte = dokładnie stare zachowanie. ZNALEZISKO UBOCZNE PODCZAS TESTOWANIA (real bug, nie fałszywy alarm): wplecenie cyklu create-natychmiast-destroy encji W ŚRODEK istniejącego, gigantycznego, kolejno-zależnego testu integracyjnego `RunScriptWorldTimePhysicsApiTest` powodowało że PÓŹNIEJSZY, NIEPOWIĄZANY `Physics.Raycast` (wywołany z Lua) trafiał w złą encję — najprawdopodobniej stan broadphase/indeks fizyki związany z natychmiastowym ponownym użyciem zwolnionego indeksu encji (nie potwierdzone do końca, poza zakresem tego zadania do zbadania) — rozwiązane przez umieszczenie testu `World.Destroy` w OSOBNEJ, świeżej scenie zamiast wplatania w współdzielony test (co jest też uczciwie lepszą praktyką testową), problem udokumentowany w `_temp.md` jako odkryte ryzyko do ewentualnego zbadania. Test: `ScriptRuntimeTests.cpp::RunWorldDestroyDeferredFlagTest` (nowy, izolowany) — pierwsze zniszczenie zwraca `destroyed=true` i faktycznie usuwa encję, DRUGIE i TRZECIE powtórzenie zwraca `destroyed=false` bez błędu (dowód idempotencji ponad dwoma powtórzeniami), `deferred=true` na żywej encji zwraca błąd i NIE niszczy encji, `deferred=false` jawnie podane zachowuje się identycznie jak domyślne wywołanie. Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wszystkie testy, zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe`+`kb_editor_tests.exe` (wszystkie exit 0).
- [x] LIB-068 Udostępnić `World.Exists`, `World.IsActive`, `World.SetActive` i `World.Name`. `World.Exists`/`World.Name` JUŻ ISTNIAŁY (zadania fundamentowe) — bez zmian. Zbadano przed implementacją: w silniku NIE ISTNIAŁO żadne ogólne pojęcie "aktywności" encji (`BehaviourComponent::enabled` i `VisibilityComponent::visible` to flagi PER-KOMPONENT o innym znaczeniu — odpowiednio wykonywanie skryptu i widoczność renderowania, nie ogólny stan encji; edytor też nie ma odpowiednika checkboxa "enabled" w hierarchii; `kb::ecs::World` nie eksponuje żadnego `Enable`/`Disable`). Decyzja architektoniczna (proporcjonalność): ZAMIAST dodawać pełnoprawny NOWY SceneComponent (co pociągnęłoby ~18 plików: rejestr komponentów, storage, mutation/query service, prefab baking/override/serializację, moduł skryptowy — dokładnie tyle ile ma dziś `VisibilityComponent`), zbadano i zreużyto ISTNIEJĄCY, LŻEJSZY wzorzec dokładnie taki jak `entityNames` w `SceneState` (`std::unordered_map<SceneEntity::IdType, std::string>` — nazwa encji TEŻ nie jest pełnoprawnym komponentem, tylko płaską mapą w SceneState) — dodano `std::unordered_set<SceneEntity::IdType> inactiveEntities` (obecność = nieaktywna, brak = aktywna domyślnie — scena bez dezaktywowanych encji płaci zero kosztu pamięci per-encja). Warstwa dostępu: `SceneEntityService::IsActive/SetActive` (2-hop: fasada→service, TA SAMA głębia co `IsAlive`, NIE 3-hop jak `Name`, bo logika jest trywialna) — `IsActive` zwraca `false` (nie rzuca) dla martwej/nigdy-nieistniejącej encji, ten sam kontrakt "nigdy nie crashuj na nieważnym uchwycie" co reszta klasy. Świadomie POZA zakresem (udokumentowane, nie ciche): propagacja hierarchiczna (nieaktywny rodzic → efektywnie nieaktywne dzieci, częsty wzorzec Unity-podobny) — sprawdzono cały plan, ŻADNE późniejsze zadanie tego nie definiuje ani nie zakłada, więc świadomie odłożone jako osobna, przyszła decyzja, nie fałszowane. Test: `ScriptRuntimeTests.cpp::RunWorldActiveStateTest` (nowy, izolowany, świeża scena — wzorem LIB-067) — domyślnie aktywna, `SetActive(false)` NIE niszczy encji (deaktywacja ≠ destrukcja), `IsActive` odzwierciedla stan, reaktywacja działa, oraz `IsActive`/`SetActive` na ZNISZCZONEJ encji zwracają odpowiednio `false`/`set=false` zamiast rzucać. Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wszystkie testy, zero błędów) oraz `kb_engine_tests.exe`+`kb_cli_tests.exe`+`kb_editor_tests.exe` (wszystkie exit 0).
- [x] LIB-069 Udostępnić `World.FindByName`, `FindByTag`, `FindAllByTag` z udokumentowanym kosztem. `World.FindByName`/`World.FindByTag` JUŻ ISTNIAŁY — dodano tylko dokumentację kosztu (komentarz przy definicji): O(n) liniowe przeszukanie przez `Transforms().ForEach`, early-exit przy pierwszym dopasowaniu, brak indeksu po nazwie/tagu. `World.FindAllByTag` — PRAWDZIWA nowa funkcja. Decyzja architektoniczna (zbadana przed implementacją): zwrócenie WIELU encji wymagałoby albo (a) kolekcji przekraczającej granicę skryptu (ScriptValue jest czysto skalarny — dokładnie ta sama, świadomie odłożona decyzja co LIB-058's uchwyt+rejestr, nie zrealizowana teraz), albo (b) iteratora indeksowego. Wybrano (b): `World.FindAllByTag(tag, skip=0) -> {entity}` — wywołujący woła wielokrotnie ze wzrastającym `skip` (0,1,2,...) aż zwrócona encja jest nieważna (ten sam konwencja invalid-entity-means-not-found co `FindByTag`/`FindByName`, nie osobna flaga `found`) — dokładnie ten sam wzorzec `{value, newState}` (stan jawnie wątkowany przez wywołującego, nie żyje po stronie silnika) co `RandomStream` z LIB-051. UCZCIWIE UDOKUMENTOWANY koszt: KAŻDE wywołanie to NIEZALEŻNY skan O(n) od początku sceny z early-exit przy (skip+1)-szym dopasowaniu — pobranie WSZYSTKICH m dopasowań z n encji kosztuje O(n·m) łącznie, nie O(n); świadomy kompromis wobec zbudowania infrastruktury kolekcji-przez-granicę, akceptowalny dla okazjonalnego zapytania "znajdź wszystkich wrogów", NIE dla hot-path na scenie z tysiącami otagowanych encji — jawnie nazwane w komentarzu, nie ukryte. Test: `ScriptRuntimeTests.cpp::RunWorldFindAllByTagTest` (nowy, izolowany, świeża scena) — 3 otagowane encje + 1 nieotagowana, pętla wywołań ze wzrastającym `skip` znajduje DOKŁADNIE 3 (nie więcej, nie mniej, bez duplikatów, bez pominięć, nigdy nie zwraca nieotagowanej encji), zapytanie o nieistniejący tag zwraca nieważną encję od razu (nie błąd), `skip` poza końcem wyników też zwraca nieważną encję (nie zawija się, nie rzuca błędu). Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wtyczki, zero błędów) oraz WSZYSTKIE 10 binarek testowych exit 0 + `kb_render_smoke.exe` (600 klatek) exit 0.
- [x] LIB-070 Udostępnić `World.InstantiatePrefab` z override'ami danych i kontrolą ownership. `World.InstantiatePrefab` JUŻ ISTNIAŁ (prefab+x/y/z→entity+count). Zbadano przed implementacją (kluczowe dla decyzji): `ScenePrefabs::Instantiate()` nie ma parametru override'ów danych; oddzielny system `ScenePrefabOverride*` to narzędzie EDYTOROWE do śledzenia "ta instancja odbiega od źródłowego prefaba" (diff+revert), którego punkty wejścia przyjmują ŚCIEŻKĘ property, NIE wartość — nie da się nim wstrzyknąć override'u przy instancjonowaniu. `kb::script::ScriptSceneComponentApi::SetProperty` JEST już w pełni generyczny na poziomie C++ (dowolna encja, nie tylko Self) — jedyną realną luką był brak wrappera skryptowego dla DOWOLNEJ encji: istniejące `Self.SetProperty` to ręcznie wpięty sugar TYLKO-LUA (`PucLuaSelfApi.cpp`), na sztywno do własnej encji wywołującego behaviour, poza standardową ścieżką `ScriptFunctionRegistry`. Decyzja: dodano `World.SetPropertyBool/Int/Float/String/Entity(entity, component, property, value) -> {set}` przez STANDARDOWĄ ścieżkę `ScriptFunctionRegistry` (Native+Lua+VisualGraph jednolicie, w przeciwieństwie do Self.SetProperty). Override'y stosowane przez KOMPOZYCJĘ (InstantiatePrefab→entity, potem SetProperty<Typ> per override), NIE atomowo w jednym wywołaniu — przekazanie LISTY override'ów wymagałoby kolekcji przez granicę skryptu, ta sama świadomie odłożona decyzja co LIB-058/069, jawnie udokumentowana w komentarzu, nie ukryta. "Kontrola ownership": zbadano że `ScenePrefabInstance` nie ma metody "zniszcz całą instancję", ALE `SceneEntityDestructionService::DestroyEntity` JUŻ KASKADOWO niszczy dzieci hierarchii — więc `World.Destroy(root)` na zwróconej encji już w pełni relinquishuje CAŁĄ zainstancjonowaną hierarchię (nie tylko root), zweryfikowane REALNYM testem z prefabem rodzic+dziecko (nie fixture z jednym obiektem jak reszta pliku) — żaden nowy kod destrukcji nie był potrzebny, tylko dowód że istniejąca kaskada działa poprawnie dla tego konkretnego uchwytu. Test: `ScriptRuntimeTests.cpp::RunWorldSetPropertyTest` (Camera.verticalFovDegrees Float + Behaviour.enabled Bool na encji INNEJ niż Self, weryfikacja realnej zmiany danych nie tylko `set=true`, plus nieznany komponent i zniszczona encja zwracają `set=false` a nie rzucają) oraz `RunWorldInstantiatePrefabOwnershipTest` (nowy prefab fixture rodzic+dziecko, `count==2`, `World.Destroy(root)` niszczy OBA — root i dziecko — dowód pełnej kaskady). Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wtyczki, zero błędów) oraz WSZYSTKIE 10 binarek testowych exit 0 + `kb_render_smoke.exe` (600 klatek) exit 0.
- [x] LIB-071 Udostępnić `Scene.Load`, `Unload`, `SetActive`, `Find`, addytywne ładowanie i progress. Zbadano przed implementacją: `SceneDocumentService::LoadIntoScene` już istniał, ale jest DESTRUKCYJNY (zawsze `ClearSceneRoots` przed instancjonowaniem `document.worldPrefab`) — brak addytywnego ładowania, brak jakiegokolwiek rejestru "załadowanych scen", brak wzorca async/progress w całym silniku (`kb::assets::AssetManager` jest w pełni SYNCHRONICZNY — zero API `Progress`/`Async`/callback). Zbadano też realną koegzystencję wielu `Scene` — POTWIERDZONO że istnieje dziś tylko jako edytorowy przypadek specjalny (`scene_`+`materialPreviewScene_`), NIE jako ogólna infrastruktura; standalone player ma dokładnie JEDNĄ `Scene` na cały proces. Decyzja architektoniczna (zamiast budować pełną wielo-instancyjną koegzystencję `Scene`, co byłoby dużą, nieuzasadnioną nową architekturą): śledzenie który root entity (i cała jego hierarchia) pochodzi z którego załadowanego dokumentu, WEWNĄTRZ JEDNEJ żywej `Scene`, przez lekki rekord na poziomie `SceneState` (`LoadedSceneRecord{id,name,path,root}` + `loadedScenes`/`nextLoadedSceneId`/`activeLoadedSceneId`) — dokładnie ten sam wzorzec "płaska mapa, nie pełny SceneComponent" co `entityNames`/`inactiveEntities` z LIB-068. Nowa warstwa: `SceneDocumentService::LoadIntoSceneAdditive` (addytywny, NIE czyści istniejących rootów, zwraca nowy root), `SceneLoadedContentService` (private, wzorzec fasada/service 2-hop: `Load/Unload/Find/Exists/Progress/SetActive/ActiveScene`), publiczna fasada `SceneLoadedContent`/`SceneLoadedContentQueries` (`Scene::LoadedContent()`), moduł skryptowy `ScriptSceneApi.cpp` (NOWY, ósmy moduł "Scene" w `EngineLibraryModule::Catalog()`, przez standardową ścieżkę `ScriptFunctionRegistry` — Native+Lua+VisualGraph jednolicie) z funkcjami `Scene.Load(path, additive=false)->{id}`, `Scene.Unload(id)->{unloaded}`, `Scene.SetActive(id)->{set}`, `Scene.GetActive()->{id}` (nowy getter-komplement do SetActive, bez którego efekt SetActive byłby nieobserwowalny ze skryptu), `Scene.Find(name)->{id}`, `Scene.LoadProgress(id)->{progress}`. UCZCIWIE UDOKUMENTOWANE ograniczenia zakresu (nie ukryte): `Scene.LoadProgress` zwraca zawsze 1.0 dla aktualnie załadowanego id i 0.0 dla nieznanego — ładowanie jest dziś synchroniczne, to realna, kompatybilna w przód powierzchnia API na przyszły loader asynchroniczny, NIE sfabrykowana wartość pośrednia; `Scene.SetActive` to WYŁĄCZNIE śledzony, odpytywalny wybór wśród załadowanych rekordów — NIE przekierowuje `World.Spawn`/`InstantiatePrefab`/itd. (przepięcie docelowej sceny każdej zarejestrowanej funkcji dynamicznie to osobna, znacznie większa zmiana, nie podjęta teraz); `additive=false` zachowuje dokładnie stare destrukcyjne zachowanie `LoadIntoScene` (bez zmian wstecznej kompatybilności). Test: `ScriptRuntimeTests.cpp::RunSceneLoadedContentApiTest` (nowy, izolowany, świeża scena — wzorzec LIB-067/068/069/070) z DWOMA prawdziwymi dokumentami scen zapisanymi na dysk (`SceneDocumentService::Save`) — non-additive `Scene.Load` zastępuje CAŁĄ wcześniejszą zawartość sceny (dowód: preexisting root znika), `Scene.GetActive`/`Scene.Find` po pierwszym ładowaniu, addytywny `Scene.Load` DOWODNIE zachowuje poprzednią zawartość (oba roota żywe jednocześnie) i NIE zmienia aktywnej sceny gdy coś już jest aktywne, `Scene.LoadProgress` zwraca 1.0 dla załadowanego i 0.0 dla nieznanego id, `Scene.SetActive` zmienia aktywną scenę i jest to widoczne przez `Scene.GetActive`, selektywny `Scene.Unload` niszczy WYŁĄCZNIE swój własny rekord (root sceny A znika, root sceny B pozostaje żywy), powtórne `Scene.Unload` na już wyładowanym id zwraca `unloaded=false` zamiast rzucać, `Scene.Load` na nieistniejącej ścieżce zwraca błąd (nie fabrykuje id). Zaktualizowano też dwa istniejące testy LIB-028 (`RunModuleInstallReportsDuplicateDiagnosticsTest`: 7→8 diagnostyk; `RunModuleCatalogTest`: katalog 7→8 modułów, dodano "Scene") oraz `RunModuleInstallCoversAllDomainsTest` o `Scene.Load` w liście oczekiwanych funkcji — uczciwa aktualizacja liczby po dodaniu ósmego modułu, nie obejście testu. Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wtyczki, zero błędów) oraz WSZYSTKIE 10 binarek testowych exit 0 (`kb_engine_tests`, `kb_ecs_api_tests`, `kb_ecs_deterministic_replay_tests`, `kb_ecs_scheduler_correctness_tests`, `kb_ecs_stress_tests`, `kb_ecs_snapshot_scale_tests`, `kb_renderer_tests`, `kb_editor_tests`, `kb_editor_material_graph_canvas_tests`, `kb_cli_tests`) + `kb_render_smoke.exe` (600 klatek) exit 0 + `kb_cli.exe api --print json` żywy zrzut katalogu potwierdzający zarejestrowane `Scene.Find`, `Scene.GetActive`, `Scene.Load`, `Scene.LoadProgress`, `Scene.SetActive`, `Scene.Unload`.
- [x] LIB-072 Zdefiniować granicę sceny persistent/gameplay oraz przeżywanie entity przy zmianie sceny. Zbadano przed implementacją: ŻADNE pojęcie "persistent" vs "gameplay" sceny nie istniało nigdzie w kodzie (grep `Persistent|DontDestroyOnLoad|KeepAlive|SurviveScene|CrossScene` w kb::scene/kb::ecs/kb::project — zero trafień poza niepowiązanym `EditorPluginCatalog::PersistentBinaryPath`, ścieżką pliku, nie cyklem życia encji). Potwierdzono w kodzie że `SceneDocumentService::ClearSceneRoots` (używane przez non-additive `Scene.Load` z LIB-071) niszczy DOSŁOWNIE każdy root, zero wyjątków — brak jakiejkolwiek flagi/tagu pomijającego wybrane encje. Potwierdzono że standalone player (`samples/standalone_player/main.cpp`) tworzy dokładnie JEDNĄ `Scene` bezpośrednio, nigdy nie woła `Scene.Load`/`SceneDocumentService` — brak istniejącej warstwy bootstrap+gameplay. Sprawdzono resztę planu: LIB-192 (`Zdefiniować runtime GameInstance żyjący ponad scenami i własność globalnych usług`) to naturalny PÓŹNIEJSZY właściciel pełnego modelu "żyje ponad scenami" — LIB-072 świadomie NIE buduje GameInstance, tylko sam mechanizm przeżywania encji, analogicznie jak LIB-071 zostawiło zdarzenia lifecycle dla LIB-073. Decyzja: reużyto DOKŁADNIE tego samego wzorca "płaska mapa w SceneState, presence=non-default" co `inactiveEntities` (LIB-068) — nowy `std::unordered_set<SceneEntity::IdType> persistentEntities`. Warstwa dostępu: `SceneEntityService::IsPersistent/SetPersistent` (2-hop, jak `IsActive`/`SetActive`), fasady `SceneEntities`/`SceneEntityQueries`, skrypt `World.IsPersistent(entity)->{persistent}` / `World.SetPersistent(entity, persistent=true)->{set}` (moduł "World", nie nowy moduł — kontynuacja LIB-065..071). `ClearSceneRoots` zmienione: pomija root oznaczony persistent (co dzięki kaskadowej destrukcji automatycznie zachowuje CAŁĄ jego hierarchię, bez osobnego śledzenia per-dziecko). UCZCIWIE UDOKUMENTOWANE ograniczenie zakresu (nie ukryte, wprost w komentarzu przy `SceneState::persistentEntities`): flaga ma efekt ochronny WYŁĄCZNIE na poziomie ROOT — oznaczenie persistent na dziecku, którego przodek-root NIE jest persistent, nie chroni tego dziecka przed kaskadową destrukcją przy czyszczeniu roota (dokładny odpowiednik ograniczenia Unity `DontDestroyOnLoad`, które też działa tylko na root-level GameObject). ZNALEZIONA I NAPRAWIONA REALNA INTERAKCJA z LIB-071 podczas implementacji (nie fałszywy alarm): `SceneLoadedContentService::Load`'s non-additive branch zakładał `RootEntities().front()` to ZAWSZE świeżo załadowany root dokumentu — założenie prawdziwe TYLKO gdy `ClearSceneRoots` niszczyło wszystko (jak przed LIB-072); z persistent survivorami obecnymi jako dodatkowe, WCZEŚNIEJSZE roots, `.front()` mógł błędnie wskazać na persistent encję zamiast nowo załadowanej treści. Naprawione przez przechwycenie zbioru rootów PRZED i PO `LoadIntoScene`, i wybranie faktycznie NOWEGO roota (różnica zbiorów) zamiast zakładania pozycji w wektorze. Test: `ScriptRuntimeTests.cpp::RunWorldPersistentStateTest` (izolowany, wzorzec LIB-068's `RunWorldActiveStateTest`: domyślnie false, SetPersistent nie niszczy/nie przenosi encji, stan odzwierciedlony natychmiast, zniszczona encja zwraca false/set=false) oraz `RunScenePersistentEntitySurvivesLoadTest` (izolowany, PRAWDZIWY test end-to-end: persistent root+dziecko + osobny non-persistent root w scenie, non-additive `Scene.Load` innego dokumentu — persistent root I jego dziecko przeżywają, non-persistent root ginie dokładnie jak przed LIB-072, `Scene.Find` po nazwie nowego dokumentu poprawnie rozwiązuje się do nowego rekordu, a `Scene.Unload` tego id niszczy WYŁĄCZNIE nowo załadowaną treść, zostawiając persistent hierarchię nietkniętą — bezpośredni dowód że naprawka wykrywania nowego roota działa poprawnie, nie tylko że `Scene.Find` zwraca jakiekolwiek id). Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wtyczki, zero błędów) oraz WSZYSTKIE 10 binarek testowych exit 0 (`kb_engine_tests`, `kb_ecs_api_tests`, `kb_ecs_deterministic_replay_tests`, `kb_ecs_scheduler_correctness_tests`, `kb_ecs_stress_tests`, `kb_ecs_snapshot_scale_tests`, `kb_renderer_tests`, `kb_editor_tests`, `kb_editor_material_graph_canvas_tests`, `kb_cli_tests`) + `kb_render_smoke.exe` (600 klatek) exit 0 + `kb_cli.exe api --print json` żywy zrzut katalogu potwierdzający zarejestrowane `World.IsPersistent`, `World.SetPersistent`.
- [x] LIB-073 Dodać lifecycle zdarzeń `SceneLoading`, `SceneLoaded`, `SceneActivated`, `SceneUnloading`, `SceneUnloaded`. Zbadano przed implementacją: istniejący `ScriptLifecycleEvent` enum (Created/Activated/Ready/.../Destroyed) to STAŁY, per-behaviour zestaw dyspozycji przez `ScriptRuntime::ExecuteLifecycle*` — NIE ogólny mechanizm nazwanych zdarzeń sceny. Potwierdzono że generyczny `Events.Subscribe`/event bus (LIB-105) NIE ISTNIEJE (nadal `[ ]` w planie, tak jak LIB-098/099) — `others/_temp.md`'s wcześniejsza notatka o tym potwierdzona aktualna. Znaleziono jednak, że REALNY, już działający mechanizm broadcastu ISTNIEJE: `kb::script::ScriptRuntime::DispatchEvent`/`DispatchEventAndDrain(scene, ScriptEvent{name, arguments}, deltaSeconds)` — broadcastuje nazwane zdarzenie do KAŻDEGO żywego, włączonego behaviour w scenie (ten sam nieukierunkowany broadcast co `context.Emit(...)` już używa wewnętrznie, LIB-011/013). Problem architektoniczny: `SceneLoadedContentService`/`ScriptSceneApi.cpp` (gdzie żyje Scene.Load/Unload/SetActive) nie mają żadnego dostępu do `kb::script::ScriptRuntime` — `kb::scene` jest warstwą NIŻSZĄ, nie zna `kb::script` (odwrócenie zależności byłoby złamaniem architektury). Decyzja: DOKŁADNIE zasada nr 3 z nagłówka planu ("Każda operacja biblioteki albo wywołuje właściwy system, albo odkłada jednoznaczną komendę do jego kolejki") — nowa lekka kolejka `SceneState::pendingSceneLifecycleEvents` (ten sam wzorzec flat-queue co `loadedScenes`), wypełniana przez `SceneLoadedContentService::Load/Unload/SetActive` w dokładnych punktach przejścia, drenowana RAZ NA KLATKĘ przez `kb::script::ScriptRuntimeSceneSystem::DispatchPendingSceneLifecycleEvents` (nowa metoda, wywoływana na początku `ExecuteStartup`/`ExecuteFrame`, PRZED `SyncBehaviourLifecycles` — "scena skończyła się ładować" obserwowalne przed Created/Activated/Ready nowych encji z tego ładowania) przez publiczną fasadę `SceneLoadedContent::DrainPendingLifecycleEvents()` (nowy publiczny typ `SceneLifecycleEventRecord{name,sceneId,sceneName}`), zamienianą na realny `ScriptEvent` z argumentami `sceneId`(Hash)/`sceneName`(String) i wysyłaną przez `DispatchEventAndDrain`. Ponieważ ładowanie jest SYNCHRONICZNE (LIB-071), `SceneLoading` i `SceneLoaded` kolejkują się razem, jedno po drugim, w tym samym wywołaniu Load — uczciwe odzwierciedlenie rzeczywistości (jak `Scene.LoadProgress`'s 1.0/0.0), nie fabrykowana asynchroniczność; jeśli ładowanie zawiedzie PO zakolejkowaniu `SceneLoading`, żaden `SceneLoaded` nie następuje — uczciwy sygnał "podjęto próbę, nie ukończono". `SceneActivated` kolejkuje się przy non-additive Load (zawsze staje się aktywna), przy additive Load TYLKO jeśli nic nie było aktywne wcześniej, oraz przy jawnym `SetActive` TYLKO gdy faktycznie zmienia aktywne id (nie duplikuje przy no-op). REALNY BŁĄD ZNALEZIONY PODCZAS PISANIA TESTU (nie w implementacji silnika, w konstrukcji testu — ale ujawniający istotną, uczciwą właściwość systemu): pierwsza wersja testu z nasłuchującym behaviour na zwykłej (nie-persistent) encji odbierała ZERO zdarzeń — przyczyna: sam nasłuchujący behaviour, jako zwykły root, był niszczony przez TĘ SAMĄ nieaddytywną `Scene.Load` co miała go powiadomić (ClearSceneRoots z LIB-072), zanim zdarzenie mogło dotrzeć — naprawione oznaczeniem nasłuchującej encji jako `persistent` (LIB-072), co jest DOKŁADNIE poprawnym, realistycznym kształtem (bootstrap/manager encja przeżywa zmianę sceny i reaguje na nią, wzorem Unity `DontDestroyOnLoad` + `SceneManager.sceneLoaded`) — nie obejście testu, tylko poprawne użycie już zbudowanego mechanizmu z LIB-072. Test: `ScriptRuntimeTests.cpp::RunSceneLifecycleEventsReachBehaviourTest` — dowodzi zdarzenia docierające do PRAWDZIWEGO, działającego behaviour przez PEŁNĄ ścieżkę produkcyjną (Scene.Load/Unload przez `ScriptFunctionRegistry::Call`, dokładnie jak wywołałby skrypt), NIE tylko bezpośrednie wywołanie `DispatchEvent` w izolacji: weryfikuje że zdarzenia NIE docierają synchronicznie wewnątrz samego wywołania Scene.Load/Unload (tylko po następnym `scene.Runtime().Update()`), że non-additive Load wysyła DOKŁADNIE SceneLoading→SceneLoaded→SceneActivated w tej kolejności z poprawnym payloadem sceneId/sceneName, oraz że Unload wysyła DOKŁADNIE SceneUnloading→SceneUnloaded. Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wtyczki, zero błędów) oraz WSZYSTKIE 10 binarek testowych exit 0 (`kb_engine_tests`, `kb_ecs_api_tests`, `kb_ecs_deterministic_replay_tests`, `kb_ecs_scheduler_correctness_tests`, `kb_ecs_stress_tests`, `kb_ecs_snapshot_scale_tests`, `kb_renderer_tests`, `kb_editor_tests`, `kb_editor_material_graph_canvas_tests`, `kb_cli_tests`) + `kb_render_smoke.exe` (600 klatek) exit 0 + `kb_cli.exe api --print json` żywy katalog nadal buduje się bez błędów (LIB-073 nie dodaje żadnych ScriptFunctionRegistry funkcji — to zdarzenia, nie funkcje).
- [x] LIB-074 Dodać test tworzenia/destrukcji 10k entity bez niestabilnego porządku i wycieków handles. OSTATNIE zadanie podsekcji "World i Scene" (LIB-065..074 teraz KOMPLETNA) — jedyne czysto testowe (nie nowe API). Zbadano przed implementacją: istniejący `kb_ecs_stress_tests` (`EcsSeededStressTests.cpp::RunSeededStructuralStressTest`) już robił seed'owany create/set/remove/destroy churn, ale na populacji ograniczonej do 512 żywych encji — NIGDY nie sięgał 10k, i nie miał ŻADNEJ asercji o wracaniu do zera żywych encji ani o stabilności kolejności iteracji. `kb::scene::SceneEntity = kb::ecs::Entity` — 64-bitowy spakowany uchwyt (`NativeArchetypeStorage.cpp::PackEntity/EntityGeneration`: `(generation << 32) | index`), generation inkrementowany przy KAŻDYM destroy — więc "wyciek uchwytu" jest wykrywalny jako: (a) zniszczona encja nadal raportująca `IsAlive()==true`, (b) TEN SAM spakowany id (index+generation razem) wydany dwukrotnie mimo reużycia indeksu po destroy. Istniejąca introspekcja: `World::NativeStorageStats().liveEntities` — już używana w `EcsSnapshotScaleTests.cpp`/`ScenePrefabInstantiationTests.cpp` do asercji dokładnej liczby żywych encji (w tym `==0` po pełnym zniszczeniu) — dokładnie odpowiedni istniejący akcesor, nie trzeba nowego. "Niestabilny porządek": brak istniejącego dedykowanego testu kolejności tworzenia/niszczenia surowych encji na poziomie kb::ecs (w przeciwieństwie do LIB-060's kontraktu deterministycznej iteracji dla `kb::library::Set/Map`) — ustalono że właściwym pojęciem jest `kb::ecs::QueryIterationOrder::Deterministic` (`QueryExecutionSettings.hpp`) kontra `StorageOrder`: `StorageOrder` może się zmieniać po swap-remove przy destroy (udokumentowany, dozwolony fakt), `Deterministic` NIE MOŻE — `QueryState.cpp::IsDeterministicExecution` wymusza SERYJNE wykonanie gdy `iterationOrder==Deterministic` (bezpieczne dla `push_back` bez synchronizacji do współdzielonego wektora bez ryzyka wyścigu — ten sam już istniejący wzorzec co `EcsQueryTests.cpp::CollectMovingSnapshot`). Test: `EcsSeededStressTests.cpp::RunEntityCreateDestroy10kStabilityTest` (nowa funkcja, dopisana do ISTNIEJĄCEGO pliku/binarki `kb_ecs_stress_tests`, limit czasu 120s wystarczający — realny czas ~1.6s) — 3 cykle churn: tworzy 10k encji (asercja `liveEntities==10000`, każdy spakowany id wstawiany do globalnego `std::unordered_set` przez CAŁY przebieg testu z asercją że wstawienie się powiodło — dowód że ŻADEN spakowany id nigdy nie powtórzył się mimo reużycia indeksów przez 3 cykle), tasuje kolejność zniszczenia (NIE FIFO — realna gra niszczy w dowolnej kolejności), niszczy połowę (asercja `!IsAlive()` dla zniszczonych, `IsAlive()` dla przeżywających, `liveEntities==survivors.size()`), zbiera kolejność `Deterministic` DWA RAZY z rzędu nad TĄ SAMĄ przeżywającą populacją i wymaga BAJT-W-BAJT identycznej sekwencji (nie tylko tego samego zbioru — to jest właściwy test "brak niestabilnego porządku"), niszczy resztę i wymaga `liveEntities==0` (dowód braku wycieku po pełnym cyklu). Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wtyczki, zero błędów) oraz WSZYSTKIE 10 binarek testowych exit 0 (`kb_engine_tests`, `kb_ecs_api_tests`, `kb_ecs_deterministic_replay_tests`, `kb_ecs_scheduler_correctness_tests`, `kb_ecs_stress_tests`, `kb_ecs_snapshot_scale_tests`, `kb_renderer_tests`, `kb_editor_tests`, `kb_editor_material_graph_canvas_tests`, `kb_cli_tests`) + `kb_render_smoke.exe` (600 klatek) exit 0.

### Komponenty i query

- [x] LIB-075 Udostępnić `Entity.Has<T>`, `TryGet<T>`, `GetRequired<T>`, `Add<T>`, `Remove<T>` tylko dla komponentów zarejestrowanych do skryptów. PIERWSZE zadanie nowej podsekcji "Komponenty i query". Zbadano przed implementacją: ISTNIEJĄCA, nieformalna lista "komponentów zarejestrowanych do skryptów" JUŻ ISTNIAŁA — `ScriptSceneComponentApi.cpp::kComponentNames` (6 nazw string: "Transform"/"Visibility"/"Camera"/"Light"/"MeshRenderer"/"Behaviour"), używana przez `World.SetProperty<Typ>`/`Self.SetProperty` (Lua/VisualGraph). Znaleziono REALNĄ asymetrię bezpieczeństwa: natywny kod skryptowy C++ (`ScriptExecutionContext::GetScene()`) ma JUŻ DZIŚ pełny, nieograniczony dostęp do `Scene&`/`World` — może wywołać `context.GetScene().Components().Cameras().TryGet(entity)` dla DOWOLNEGO komponentu, całkowicie omijając 6-nazwową allowlistę, którą Lua/VisualGraph już mają wymuszoną. `Entity` w treści zadania = `kb::library::EntityHandle` (istniejący od LIB-005/008, opakowuje `SceneEntity`+`sceneId`, wymieniony w nagłówku planu jako jeden z trzech stabilnych uchwytów obok AssetRef/SceneRef) — NIE surowy `kb::ecs::Entity` (ten ma już w pełni generyczne, NIEOGRANICZONE `Has<T>/TryGet<T>/Remove<T>` na `World`, ale to poziom silnika, nie kontrakt biblioteki). `kb::ecs::World::Has<T>/TryGet<T>` istniały już wcześniej — LIB-075 NIE buduje nowego storage, tylko OGRANICZONY WRAPPER wokół już-otwartego dostępu. LIB-076 (pełny registry komponentów: schema/wersja/serializacja/thread policy) potwierdzone jako NIE hard-blocker — żadne późniejsze zadanie (LIB-077+) nie wymaga identycznego mechanizmu z LIB-075; zastosowano DOKŁADNIE ten sam wzorzec "proporcjonalna, ręcznie utrzymywana allowlista teraz, prawdziwy registry później" co LIB-068's `inactiveEntities`. Decyzja architektoniczna: `template<typename Component> struct ScriptComponentAccess` (nowy plik `EngineLibraryScriptComponentAccess.hpp`) — PRIMARY template niezdefiniowany funkcjonalnie, wywołuje `static_assert` z czytelnym komunikatem przy próbie instancjacji dla NIEZAREJESTROWANEGO typu (błąd KOMPILACJI, nie runtime — najsilniejsza możliwa egzekucja "tylko dla zarejestrowanych komponentów", dostępna WŁAŚNIE dlatego że to natywny kod C++ — Lua/VisualGraph nie potrafią nawet wyrazić szablonów, więc nigdy nie dotykają tej ścieżki). 6 jawnych specjalizacji (Transform/Visibility/Camera/Light/MeshRenderer/Behaviour), DOKŁADNIE te same nazwy co `kComponentNames`. Deklaracje szablonowych metod `EntityHandle::Has/TryGet/GetRequired/Add/Remove` w LEKKIM `EngineLibraryEntityHandle.hpp` (bez dociągania nagłówków typów komponentów), DEFINICJE (ciała szablonów) w osobnym `EngineLibraryScriptComponentAccess.hpp` — wywołujący musi dołączyć OBA nagłówki żeby faktycznie zainstancjonować szablon dla realnego typu, co utrzymuje lekkość `EngineLibraryEntityHandle.hpp` dla kodu który go używa tylko do `IsAlive`/`Validate`. Obsłużono heterogeniczność komponentów: Transform/Visibility są MANDATORY (każda encja ma je od utworzenia, brak `Remove()` na fasadzie) — `Remove<T>` dla nich UCZCIWIE zwraca `false` zamiast cichego no-op albo wywołania nieistniejącej metody fasady; `Add<T>` dla nich nadal działa (semantyka Set/nadpisania, nie "fail jeśli już istnieje"). Camera/Light/MeshRenderer/Behaviour są OPCJONALNE — `Remove<T>` faktycznie usuwa i zwraca `true`/`false` zależnie od tego czy komponent istniał. `GetRequired<T>` zwraca `kb::library::Result<Component>` (istniejący typ z LIB-032/061, T-lub-ScriptError) z `LibraryErrorCode::InvalidArgument` (brak dedykowanego kodu "component missing" w enumie — udokumentowano wybór wprost w komentarzu, nie ukryto). Martwy/obcej-sceny uchwyt: `Has`/`TryGet`/`Add`/`Remove` zwracają `false`/`nullptr` (nigdy nie rzucają — ten sam kontrakt co `IsAlive`), `GetRequired` zwraca failed `Result`. Test: `EngineLibraryTests.cpp::RunEntityHandleScriptComponentAccessTest` — pełny cykl na komponencie opcjonalnym (Camera: brak→Add→Has/TryGet/GetRequired zwracają prawdziwe dane→mutowalny TryGet faktycznie zapisuje do ŻYWEGO komponentu, nie kopii→Remove usuwa i zwraca true→ponowny Remove zwraca false) ORAZ na mandatory (Transform: zawsze obecny, Add nadpisuje dane, Remove zwraca false i NIC nie usuwa) ORAZ martwa encja (wszystkie 5 operacji zwraca false/nullptr/failed Result, zero wyjątków). Build+testy zielone: PEŁNY `cmake --build . --config Debug` (silnik+renderer+editor+narzędzia+wtyczki, zero błędów) oraz WSZYSTKIE 10 binarek testowych exit 0 (`kb_engine_tests`, `kb_ecs_api_tests`, `kb_ecs_deterministic_replay_tests`, `kb_ecs_scheduler_correctness_tests`, `kb_ecs_stress_tests`, `kb_ecs_snapshot_scale_tests`, `kb_renderer_tests`, `kb_editor_tests`, `kb_editor_material_graph_canvas_tests`, `kb_cli_tests`) + `kb_render_smoke.exe` (600 klatek) exit 0.
- [ ] LIB-076 Zdefiniować registry komponentów: schema, wersja, serializacja, thread policy, read/write capability.
- [ ] LIB-077 Udostępnić bezpieczne pola komponentów przez generated accessors, nie przez dowolny reflection write.
- [ ] LIB-078 Dodać `Query<T...>` wyłącznie do faz dopuszczających iterację i z zakazem structural change w pętli.
- [ ] LIB-079 Dodać `Query.With`, `Without`, `Any`, `ChangedSince`, `Enabled` oraz stable order opt-in.
- [ ] LIB-080 Dodać batch command buffer dla `Spawn`, `Destroy`, Add/Remove component i przypisania tagów.
- [ ] LIB-081 Dodać `ComponentChanged` event z limitem, coalescingiem i bez alokacji per zmiana.
- [ ] LIB-082 Dodać walidator, że typ komponentu nie wystawia surowego adresu pamięci do Lua/graph.
- [ ] LIB-083 Dodać test aliasingu query, entity destroyed in query i command flush boundary.
- [ ] LIB-084 Udostępnić inspector-friendly metadata komponentów bez uczynienia edytora warunkiem runtime.

### Transform i hierarchia

- [ ] LIB-085 Udostępnić `Transform.LocalPosition`, `LocalRotation`, `LocalScale`, `WorldPose` i `SetWorldPose`.
- [ ] LIB-086 Udostępnić `Transform.Parent`, `SetParent(parent, keepWorld)` i kontrolę cykli hierarchii.
- [ ] LIB-087 Udostępnić `Transform.GetChild`, `ChildCount`, `FindChild` oraz iterację bez alokacji.
- [ ] LIB-088 Udostępnić `Translate`, `Rotate`, `LookAt`, `TransformPoint`, `InverseTransformPoint`.
- [ ] LIB-089 Zdefiniować kiedy world matrix jest aktualizowana po modyfikacji i jak to obserwuje renderer/fizyka.
- [ ] LIB-090 Dodać `TransformChanged` z informacją local/world i bez event storm.
- [ ] LIB-091 Dodać test zachowania `keepWorld`, parent destroy, deep hierarchy i zero/ujemnej skali.
- [ ] LIB-092 Zdefiniować serializację prefab override dla transformacji i hierarchy.

## 7. Czas, harmonogram, timery i asynchroniczność

- [ ] LIB-093 Udostępnić `Time.Delta`, `UnscaledDelta`, `FixedDelta`, `Elapsed`, `FrameIndex`, `FixedStepIndex`.
- [ ] LIB-094 Udostępnić `Time.Scale`, pause domains i jawne zasady zachowania `FixedTick` podczas pause.
- [ ] LIB-095 Udostępnić `Timer.Once`, `Timer.Repeat`, `Timer.Cancel`, `Timer.Pause`, `Timer.Resume` i owner entity.
- [ ] LIB-096 Zdefiniować kolejność timerów o tym samym czasie oraz catch-up po długiej klatce.
- [ ] LIB-097 Udostępnić `Coroutine`/`Task` tylko po wyborze modelu: generator Lua, state machine graph i C++ task adapter.
- [ ] LIB-098 Zaimplementować yield na sekundy, fixed steps, event, asset load i scene load bez blokowania main thread.
- [ ] LIB-099 Zaimplementować cancellation propagated przy Deactivated/Destroyed/scene unload.
- [ ] LIB-100 Dodać `AsyncResult<T>` z sukcesem, błędem, anulowaniem i callbackiem na właściwym wątku.
- [ ] LIB-101 Dodać watchdog na nieskończone coroutine/graph loop i diagnostykę miejsca utworzenia.
- [ ] LIB-102 Dodać test deterministyczności timerów i anulowania w tej samej fazie.

## 8. Zdarzenia, komunikaty i sygnały

- [ ] LIB-103 Rozdzielić zdarzenia lokalne entity, zdarzenia świata i komunikaty między systemami.
- [ ] LIB-104 Zdefiniować typowany `EventId`/schema zamiast dowolnych string payloadów w hot path.
- [ ] LIB-105 Udostępnić `Events.Subscribe`, `Unsubscribe`, `Emit`, `EmitDeferred` i `Broadcast`.
- [ ] LIB-106 Dodać filtry odbiorcy: entity, tag, komponent, scene, player i channel.
- [ ] LIB-107 Zdefiniować synchroniczne vs deferred dispatch i zakazać niejawnego mieszania.
- [ ] LIB-108 Dodać event payload value types, limity rozmiaru oraz wersjonowanie schema.
- [ ] LIB-109 Dodać `Signal<T>` dla lokalnej obserwacji bez globalnego busa.
- [ ] LIB-110 Dodać telemetry: liczba subskrypcji, dispatch duration, dropped/invalid events.
- [ ] LIB-111 Dodać test kolejności, unsubscribe during dispatch, destroyed owner i recursive emit.
- [ ] LIB-112 Dodać bridge zdarzeń gameplay do Visual Graph z typowanymi pinami.

## 9. Input i urządzenia

- [ ] LIB-113 Zdefiniować asset `InputActionMap`, action, binding, composite, deadzone i rebinding.
- [ ] LIB-114 Udostępnić `Input.ActionBool`, `ActionFloat`, `Action2D`, `Pressed`, `Released`, `Held`.
- [ ] LIB-115 Udostępnić akcje per player/local user, a nie jeden globalny singleton wejścia.
- [ ] LIB-116 Udostępnić urządzenia keyboard/mouse/gamepad/touch tylko przez normalizowany model input.
- [ ] LIB-117 Udostępnić `Pointer.Position`, delta, scroll, button i ray z aktywnej kamery.
- [ ] LIB-118 Udostępnić input contexts/priorities dla UI, gameplay, konsoli i debug overlay.
- [ ] LIB-119 Udostępnić rebinding runtime z walidacją konfliktu i serializacją ustawień użytkownika.
- [ ] LIB-120 Obsłużyć focus lost, device disconnect, background window i reset stanów pressed.
- [ ] LIB-121 Dodać recording/replay input do testów deterministycznych.
- [ ] LIB-122 Dodać test parity action state w FixedTick/Tick i między native/Lua/graph.

## 10. Fizyka, kolizje i ruch

- [ ] LIB-123 Zdefiniować komponenty i API `RigidBody`, `Collider`, `CharacterController`, `Joint`, `PhysicsMaterial`.
- [ ] LIB-124 Udostępnić force, impulse, velocity, angular velocity, kinematic move i sleep/wake.
- [ ] LIB-125 Udostępnić `Raycast`, sphere/box/capsule cast, overlap i closest point z warstwą maski.
- [ ] LIB-126 Udostępnić wersje `NonAlloc` zapytań i wymaganie bufora przy użyciu w ticku.
- [ ] LIB-127 Udostępnić `OnCollisionEnter/Stay/Exit` i `OnTriggerEnter/Stay/Exit` z deterministycznym payloadem.
- [ ] LIB-128 Zdefiniować relację między `FixedTick`, command buffer a momentem wykonania symulacji fizyki.
- [ ] LIB-129 Udostępnić collision layers, query layers i matrix interakcji jako asset/projekt config.
- [ ] LIB-130 Dodać API constraints/joints z limitem dostępnych, faktycznie obsługiwanych typów.
- [ ] LIB-131 Dodać API charakteru: slope limit, step offset, grounding, platform motion i gravity.
- [ ] LIB-132 Dodać debug draw fizyki i trace pojedynczego query bez wpływu na release path.
- [ ] LIB-133 Przetestować fast mover, spawn/despawn collider, parented rigidbody i scene unload.
- [ ] LIB-134 Przetestować determinism claim wyłącznie tam, gdzie backend fizyki go realnie gwarantuje.

## 11. Renderowanie, kamera, materiały i efekty

- [ ] LIB-135 Udostępnić `Camera` z pose, projection, FOV/ortho, near/far, viewport i priority.
- [ ] LIB-136 Udostępnić target renderowania, clear flags, layer mask i aktywną kamerę per player/view.
- [ ] LIB-137 Udostępnić `MeshRenderer` tylko przez istniejące assety mesh/material i bez ręcznego GPU ownership.
- [ ] LIB-138 Udostępnić sloty materiałów zgodnie z sekcjami mesha oraz `MaterialOverride` per slot.
- [ ] LIB-139 Udostępnić `MaterialInstance` jako instancję runtime z explicite lifetime i limitem wariantów.
- [ ] LIB-140 Udostępnić bezpieczny set shader parameterów po nazwie/ID walidowanym względem graph/material schema.
- [ ] LIB-141 Udostępnić `Light`, shadow policy, color temperature/intensity i layer mask, jeśli renderer je obsługuje.
- [ ] LIB-142 Udostępnić post-process volume/profile wyłącznie z assetowym, serializowalnym zestawem parametrów.
- [ ] LIB-143 Udostępnić particles/VFX jako assetową instancję: play, stop, seed, parameter, event.
- [ ] LIB-144 Udostępnić `Renderer.IsVisible`, bounds i frustum test bez wymuszania synchronizacji GPU.
- [ ] LIB-145 Dodać screen/world conversions, ray z kamery i screen capture przez kontrolowaną async ścieżkę.
- [ ] LIB-146 Dodać test lifecycle render resource przy destroy, scene unload i asset reload.

## 12. Audio, haptics i multimedia

- [ ] LIB-147 Zdefiniować `AudioClip`, `AudioBus`, `AudioMixer`, `AudioSource`, `AudioListener` i `Snapshot`.
- [ ] LIB-148 Udostępnić `Audio.Play`, `Stop`, pause, seek, volume, pitch, loop, spatial blend i priority.
- [ ] LIB-149 Udostępnić one-shot z owner entity i automatycznym cleanupem, bez wycieku source.
- [ ] LIB-150 Udostępnić mixer parameter i snapshot transition z walidacją assetu.
- [ ] LIB-151 Udostępnić occlusion/obstruction tylko po istniejącym backendzie i z kontrolą kosztu raycastów.
- [ ] LIB-152 Udostępnić audio events/markers i synchronizację gameplay bez polegania na niedokładnym wall clock.
- [ ] LIB-153 Udostępnić haptics per device/user przez capability i limity platformy.
- [ ] LIB-154 Dodać test asset unload podczas odtwarzania, scene change i pooled one-shots.

## 13. Assety, prefaby, ładowanie i serializacja

- [ ] LIB-155 Udostępnić `Assets.Load<T>`, `LoadAsync<T>`, `IsLoaded`, `Unload` z ownership handle.
- [ ] LIB-156 Udostępnić `Assets.Find` wyłącznie po stable ID/logicznej ścieżce projektu, nigdy po fizycznej ścieżce OS.
- [ ] LIB-157 Dodać typowane referencje: mesh, material, texture, audio, prefab, scene, animation, graph, input map.
- [ ] LIB-158 Zdefiniować cache, reference count, weak reference i unload policy dla assetów runtime.
- [ ] LIB-159 Dodać walidację asset compatibility i czytelną diagnostykę brakującej zależności.
- [ ] LIB-160 Udostępnić prefab instantiate z parametrami, parentem, transformem i callbackiem ukończenia.
- [ ] LIB-161 Zdefiniować prefab variants/overrides oraz kolejność zastosowania override'ów.
- [ ] LIB-162 Udostępnić `SaveGame` z wersjonowanymi schema, migracją i atomowym zapisem.
- [ ] LIB-163 Rozdzielić SaveGame, ustawienia użytkownika, stan sceny i dane sieciowe.
- [ ] LIB-164 Dodać limit wielkości zapisu, integrity hash i diagnostykę uszkodzonego pliku.
- [ ] LIB-165 Dodać test round-trip serializacji dla każdej publicznej wartości i referencji assetu.

## 14. Animacja, skinned mesh i cinematic

- [ ] LIB-166 Zdefiniować `Animator`, `AnimationClip`, `AnimatorController`, parameters i layer mask.
- [ ] LIB-167 Udostępnić play/crossfade, speed, normalized time, bool/int/float/trigger parameter i state query.
- [ ] LIB-168 Udostępnić animation events przez wersjonowany, typowany payload, bez reflection invoke po stringu.
- [ ] LIB-169 Udostępnić root motion z jasnym właścicielem: animator, character controller albo rigidbody.
- [ ] LIB-170 Udostępnić blend trees, IK targets i rig constraints tylko po istniejącej implementacji runtime.
- [ ] LIB-171 Udostępnić timeline/sequencer: play, pause, seek, bindings, marker events i skip semantics.
- [ ] LIB-172 Dodać test unload clip/controller podczas animacji i rebind po prefab instantiate.

## 15. UI i interakcja użytkownika

- [ ] LIB-173 Zdefiniować runtime UI tree, `UIDocument`, element ID, style asset i data binding boundary.
- [ ] LIB-174 Udostępnić create/destroy/show/hide elementów przez kontrolowaną kolejkę UI.
- [ ] LIB-175 Udostępnić text, image, button, toggle, slider, list, input field, scroll view i modal dialog.
- [ ] LIB-176 Udostępnić eventy click, pointer, submit, changed, focus, navigation z ownerem i unsubscribe.
- [ ] LIB-177 Udostępnić `UI.Find` tylko jako setup API; hot path ma cache'ować typed handle.
- [ ] LIB-178 Udostępnić data binding jednokierunkowy/dwukierunkowy z ochroną przed feedback loop.
- [ ] LIB-179 Udostępnić list virtualization i pooling zamiast tworzenia setek elementów per frame.
- [ ] LIB-180 Dodać input routing UI/gameplay, focus management, gamepad navigation i accessibility hooks.
- [ ] LIB-181 Dodać localization keys, formatowanie pluralizacji i fallback language bez stringów hardcoded w logice.
- [ ] LIB-182 Dodać test UI event po destroy, async scene transition i multi-player viewport.

## 16. Nawigacja, AI i zachowania

- [ ] LIB-183 Zdefiniować `NavMesh`, `NavAgent`, `NavObstacle`, query filter i area cost.
- [ ] LIB-184 Udostępnić path request sync/async, destination, stop, velocity, path status i remaining distance.
- [ ] LIB-185 Udostępnić steering/avoidance z jednoznaczną relacją do physics/character controller.
- [ ] LIB-186 Udostępnić percepcję: sight, hearing, proximity, team/filter i eventy z ograniczonym kosztem.
- [ ] LIB-187 Zaprojektować behaviour tree/state machine/utility AI jako asset runtime, nie jako drugi scheduler skryptów.
- [ ] LIB-188 Udostępnić blackboard z typowanymi kluczami, scope entity/team/world i serializacją.
- [ ] LIB-189 Udostępnić GOAP tylko po osobnym benchmarku i decyzji o kosztach; nie mieszać z BT w MVP.
- [ ] LIB-190 Dodać deterministic seed dla AI i debug snapshot decyzji.
- [ ] LIB-191 Dodać test path invalidation, target destroy i navmesh scene unload.

## 17. Gameplay framework

- [ ] LIB-192 Zdefiniować runtime `GameInstance` żyjący ponad scenami i własność globalnych usług.
- [ ] LIB-193 Zdefiniować `GameMode` jako reguły serwera/single-player, nie komponent dowolnego entity.
- [ ] LIB-194 Zdefiniować `GameState` jako replikowalny stan meczu/partii.
- [ ] LIB-195 Zdefiniować `Player`, `PlayerController`, `Pawn/Character`, `PlayerState` i lifecycle join/leave.
- [ ] LIB-196 Zdefiniować `CameraManager` i policy camera possession/follow/spectate.
- [ ] LIB-197 Dodać `SpawnPoint`, respawn policy, team assignment i match phases jako asset/config.
- [ ] LIB-198 Dodać `Tag`, `Layer`, `Team`, `Faction` z jednolitymi filtrami dla AI, physics, render i damage.
- [ ] LIB-199 Dodać system damage/heal z typowanym `DamageEvent`, source, instigator, hit i odpornościami.
- [ ] LIB-200 Dodać health, attributes, inventory, equipment i pickup jako opcjonalne moduły, nie wbudowane pola entity.
- [ ] LIB-201 Dodać gameplay effects/abilities z cooldown, cost, target rules i cancellation.
- [ ] LIB-202 Dodać checkpoint, game flow, pause, restart, win/lose i transition scene.
- [ ] LIB-203 Dodać sample: third-person controller, top-down controller, platformer i simple shooter.

## 18. Sieć i multiplayer

Sieć wymaga oddzielnej decyzji architektonicznej. Nie wolno eksponować
pozornie działającego `Network.*`, dopóki nie ma authority, transportu,
serializacji i modelu replikacji.

- [ ] LIB-204 Wybrać model: authoritative server, listen server, peer-to-peer lub offline-only w pierwszym wydaniu.
- [ ] LIB-205 Zdefiniować `NetworkRole`, `NetworkObject`, owner, authority i lifecycle spawn/despawn.
- [ ] LIB-206 Zdefiniować schema replikacji komponentów z quantization, delta i wersjonowaniem.
- [ ] LIB-207 Udostępnić RPC reliable/unreliable z kierunkiem client→server/server→client i walidacją ownership.
- [ ] LIB-208 Udostępnić network variables z hookiem zmiany i bez automatycznej replikacji dowolnych obiektów.
- [ ] LIB-209 Zdefiniować input command, snapshot, interpolation, prediction i reconciliation przed API ruchu multiplayer.
- [ ] LIB-210 Wprowadzić kontrolę czasu, tick rate, packet budget i backpressure.
- [ ] LIB-211 Dodać zabezpieczenia: validate server side, rate limits, payload size, spoofing i deserialization bounds.
- [ ] LIB-212 Dodać replay/network simulation: latency, jitter, loss, reorder i disconnect.
- [ ] LIB-213 Dodać test host/client, late join, reconnect, despawn during RPC i schema mismatch.

## 19. Platforma, pliki, system i usługi

- [ ] LIB-214 Udostępnić `Platform` tylko przez capability: język, region, path do user data, clipboard, URL, vibration.
- [ ] LIB-215 Zakazać skryptom dowolnego odczytu/zapisu systemu plików; udostępnić sandbox `UserStorage`.
- [ ] LIB-216 Udostępnić atomowy `UserStorage.Read/Write/Delete/List` z quota i async API.
- [ ] LIB-217 Udostępnić settings audio/video/input z transakcją apply/revert i walidacją możliwości urządzenia.
- [ ] LIB-218 Udostępnić achievements, cloud save, DLC i platform user tylko jako opcjonalne adaptery capability.
- [ ] LIB-219 Udostępnić locale, time zone i safe date-time tylko w granicach potrzeb gry.
- [ ] LIB-220 Dodać test sandbox escape, quota, atomic write failure i capability unavailable.

## 20. Diagnostyka, debug i narzędzia twórcy

- [ ] LIB-221 Udostępnić `Log.Trace/Debug/Info/Warn/Error` z kategorią, entity, world i rate limiting.
- [ ] LIB-222 Udostępnić structured fields zamiast składania każdego loga przez format string.
- [ ] LIB-223 Udostępnić `Assert`, `Require`, `SoftFail` z polityką development/release i stack trace skryptu.
- [ ] LIB-224 Udostępnić `Debug.DrawLine/Ray/Box/Sphere/Text` z duration, channel i build guard.
- [ ] LIB-225 Udostępnić profiler scopes, counters, timeline events i allocation counters bez wpływu na release.
- [ ] LIB-226 Udostępnić console commands z typowaną argumentacją, permissions i help generowanym z manifestu.
- [ ] LIB-227 Udostępnić runtime inspector read-only dla entity, componentów, timerów, subscriptions i graph execution.
- [ ] LIB-228 Udostępnić controlled hot reload Lua/Visual Graph z migracją state albo jawnym restartem behaviour.
- [ ] LIB-229 Udostępnić breakpoints/step dla Visual Graph oraz source location dla Lua errors.
- [ ] LIB-230 Dodać crash report z wersją API, assetami, błędem i ostatnimi zdarzeniami bez danymi poufnymi.

## 21. Wydajność, wielowątkowość i determinism

- [ ] LIB-231 Oznaczyć każdą funkcję: main thread, worker-safe, render-thread command albo forbidden in phase.
- [ ] LIB-232 Zdefiniować read snapshots i command queues dla pracy równoległej bez data races.
- [ ] LIB-233 Dodać budget wykonania Lua/Visual Graph per frame/behaviour oraz policy przekroczenia.
- [ ] LIB-234 Dodać allocation budget i telemetry per moduł biblioteki.
- [ ] LIB-235 Przenieść konwersje string→ID poza hot path, cache'ować bindingi i query plans.
- [ ] LIB-236 Dodać `NonAlloc`/batch warianty raycast, find, event emit i transform read tam, gdzie benchmark to uzasadnia.
- [ ] LIB-237 Zdefiniować deterministic subset biblioteki: random, timers, order, input replay, fixed simulation.
- [ ] LIB-238 Oznaczyć API niedeterministyczne (wall time, platform, async IO, rendering) w metadata i Visual Graph.
- [ ] LIB-239 Dodać benchmarki spawn/query/transform/event/physics call dla 1k, 10k i 100k entity.
- [ ] LIB-240 Dodać test ThreadSanitizer/ASan lub odpowiedni Windows-equivalent dla kolejki komend i lifetime handles.

## 22. Parzystość C++, Lua i Visual Graph

- [ ] LIB-241 Ustalić kanoniczne nazwy modułów: `World`, `Entity`, `Transform`, `Time`, `Math`, `Input`, `Physics`, `Audio`, `Assets`.
- [ ] LIB-242 Wygenerować Lua module table z katalogu, a nie pisać ręczne aliasy odrębne od C++.
- [ ] LIB-243 Wygenerować definicje node/pin Visual Graph z tych samych sygnatur.
- [ ] LIB-244 Dodać typy konwersji graph tylko, gdy nie tracą informacji albo wyświetlają jawne ostrzeżenie.
- [ ] LIB-245 Dodać przykład i test każdego API w C++, Lua i graph, z wyjątkiem celowo native-only API.
- [ ] LIB-246 Oznaczyć native-only/authoring-only/server-only API w manifeście i ukryć je z niewłaściwego frontend-u.
- [ ] LIB-247 Dodać source map: node/pin → funkcja katalogu → implementacja runtime → dokumentacja.
- [ ] LIB-248 Dodać generator Markdown/reference z manifestu, aby dokumentacja nie starzała się niezależnie od kodu.
- [ ] LIB-249 Dodać autocompletion Lua i podpowiedzi node search z opisem, kategorią, przykładami i wersją.
- [ ] LIB-250 Dodać test, że nazwa, podpis i semantics opisane w docs odpowiadają manifestowi builda.

## 23. Testy, jakość i Definition of Done

- [ ] LIB-251 Stworzyć osobny target testów `Engine21kbLibrary` bez zależności od UI edytora.
- [ ] LIB-252 Dodać testy unit dla typów, marshalling, handles, errors i metadata.
- [ ] LIB-253 Dodać testy integracyjne runtime dla lifecycle, command buffer, scene i prefab.
- [ ] LIB-254 Dodać golden tests Visual Graph: graph asset → binding → runtime result.
- [ ] LIB-255 Dodać testy Lua dla błędów typu, życia entity, coroutine cancellation i stack trace.
- [ ] LIB-256 Dodać testy property/fuzz dla parserów, asset refs, event payload i serializacji save.
- [ ] LIB-257 Dodać test deterministycznego replay dla minimalnej sceny gameplay.
- [ ] LIB-258 Dodać testy soak: spawn/destroy, load/unload scene, subscribe/unsubscribe, hot reload.
- [ ] LIB-259 Dodać testy performance jako progi regresji, oddzielnie od testów funkcjonalnych.
- [ ] LIB-260 Wymagać clean build bez warningów własnego kodu i bez nowych warningów zależności w CI.
- [ ] LIB-261 Wymagać `git diff --check`, testów właściwego targetu i przeglądu manifest compatibility dla każdej zmiany API.
- [ ] LIB-262 Utrzymywać matrix platform/backend/frontend dla funkcji, zamiast deklarować wsparcie bez testu.

## 24. Kolejność realizacji

### M0 — kontrakt i działający pionowy wycinek

Wykonać LIB-001..015, 016..030, 031..040, 041..056, 065..092,
093..112, 113..122 oraz tylko niezbędne 123..134 i 155..161. Rezultat:
nowy projekt może stworzyć prefab gracza, sterować nim, wykonywać raycast,
reagować na kolizję, odtworzyć dźwięk, spawnąć prefab i przeładować scenę — w
Native, Lua i Visual Graph.

### M1 — kompletna gra single-player

Wykonać rendering/kamerę (135..146), UI (173..182), animację (166..172),
gameplay framework (192..203), save/settings (162..165, 214..220) oraz
diagnostykę (221..230). Rezultat: menu, gameplay loop, stan gry, zapis,
debugowanie i pakietowanie są funkcjami runtime, nie trikami edytora.

### M2 — skala i narzędzia produkcyjne

Wykonać AI/nawigację (183..191), pełną observability, hot reload, benchmarki,
budżety i parity docs (231..250). Rezultat: zespół może tworzyć i diagnozować
większą grę bez niekontrolowanego kosztu skryptów.

### M3 — multiplayer tylko po decyzji o modelu

Wykonać 204..213 po zatwierdzeniu transportu, authority i targetowych
platform. Multiplayer nie blokuje M0–M2 i nie może wymuszać przypadkowej
architektury na local gameplay.

## 25. Referencje projektowe

Poniższe dokumentacje są inspiracją dla zakresu i semantyki, nie specyfikacją
do kopiowania. Engine21kb utrzymuje własne kontrakty, nazwy i ograniczenia
zgodne z własnym runtime'em.

- [Unity — MonoBehaviour i lifecycle](https://docs.unity3d.com/2023.1/Documentation/ScriptReference/MonoBehaviour.html)
- [Unity — script execution order](https://docs.unity3d.com/Manual/execution-order.html)
- [Unreal Engine — Gameplay Framework](https://dev.epicgames.com/documentation/en-us/unreal-engine/gameplay-framework-in-unreal-engine)
- [Unreal Engine — Blueprint foundations](https://dev.epicgames.com/documentation/en-us/unreal-engine/blueprint-foundations)
- [Unreal Engine — gameplay systems](https://dev.epicgames.com/documentation/en-us/unreal-engine/gameplay-systems-in-unreal-engine)
- [Godot — overridable functions i lifecycle Node](https://docs.godotengine.org/en/stable/tutorials/scripting/overridable_functions.html)
- [Godot — high-level multiplayer](https://docs.godotengine.org/en/stable/tutorials/networking/high_level_multiplayer.html)

## 26. Kryterium ukończenia biblioteki

Biblioteka nie jest „ukończona” po stworzeniu listy klas ani po pokazaniu
węzłów w edytorze. Dany moduł jest gotowy wyłącznie, gdy ma realny backend
runtime, jawne ownership i lifecycle, parity wspieranych frontendów,
diagnostykę, testy błędów oraz test integracyjny wykonany w Play Mode.
